<phase>17</phase>
<plan>01</plan>
<title>AUTOSAR arxml parser and version extraction bug fixes</title>

<objective>
Fix BUG-01, BUG-02, and BUG-03 discovered during v1.0.17 AUTOSAR verification:
- BUG-01: .arxml files detected but never parsed for dependencies
- BUG-02: SWC type definition elements not extracted from per-component arxml files
- BUG-03: AUTOSAR dependencies always show "unspecified" instead of real versions from .epd and Doxygen headers
Also fix duplicate/shadow issues where system linker deps conflicted with AUTOSAR ecosystem entries.
</objective>

<tasks>
1. BUG-01: Create arxml parser (src/parsers/c/arxml.rs) extracting SHORT-NAME from SW-COMPONENT-PROTOTYPE and BSW-MODULE-DESCRIPTION
2. BUG-02: Extend arxml parser to extract SWC type definition elements (APPLICATION-SW-COMPONENT-TYPE, ECU-ABSTRACTION-SW-COMPONENT-TYPE, etc.)
3. BUG-03: Parse REVISION-LABEL from .epd files and SW Version from Doxygen C/H headers; populate real versions
4. Fix dedup: drop system linker deps shadowed by autosar ecosystem entries
5. Post-walk pass: upgrade system deps to autosar ecosystem using epd and doxygen versions
</tasks>
