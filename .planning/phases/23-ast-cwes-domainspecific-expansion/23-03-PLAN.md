---
phase: 23-ast-cwes-domainspecific-expansion
plan: 03
type: execute
wave: 3
depends_on: [23-02]
files_modified:
  - benchmark/juliet/ast.json
  - benchmark/juliet/ANALYSIS.md
autonomous: true
requirements: [CWEXP-03]

must_haves:
  truths:
    - "Juliet benchmark re-run produces an updated benchmark/juliet/ast.json that includes findings for the Juliet-tractable Phase 23 CWEs (114, 272, 284, 479, 785)"
    - "benchmark/juliet/ANALYSIS.md contains 8 new per-CWE TP/FP rows for CWEs 114, 272, 284, 427, 479, 591, 762, 785"
    - "ANALYSIS.md documents the synthetic-TP CWEs (CWE-427, 591, 762) with explicit notes per D-11"
    - "ANALYSIS.md documents no regression on the existing 41 CWEs"
    - "ANALYSIS.md total CWE count updated to 49"
  artifacts:
    - path: "benchmark/juliet/ast.json"
      provides: "Regenerated AST scanner findings on the Juliet corpus including Phase 23 CWEs"
    - path: "benchmark/juliet/ANALYSIS.md"
      provides: "Final 49-CWE per-CWE TP/FP table with Phase 23 rows added"
      contains: "CWE-114"
    - path: "benchmark/juliet/ANALYSIS.md"
      contains: "CWE-785"
  key_links:
    - from: "sc2sbom CLI run on Juliet corpus"
      to: "benchmark/juliet/ast.json"
      via: "existing benchmark driver (cargo run / regenerate.sh / documented command)"
      pattern: "ast\\.json"
    - from: "benchmark/juliet/ast.json"
      to: "benchmark/juliet/ANALYSIS.md"
      via: "file-level oracle (CWEdir vs finding.cwe_id)"
      pattern: "CWE-(114|272|284|427|479|591|762|785)"
---

<objective>
Re-run the Juliet corpus benchmark with the 49-CWE rule set in place and update `benchmark/juliet/ANALYSIS.md` with 8 new per-CWE rows reflecting Phase 23 coverage. Per D-10 (validate-after-implement) and ROADMAP Phase 23 success criterion #5.

Purpose: Phase 23 success criteria require (a) ≥1 TP per CWE where Juliet coverage exists (synthetic fixture TPs are acceptable per D-11 for CWE-427/591/762), (b) FP% ≤40% where Juliet coverage exists, (c) no regression on existing 41 CWEs, and (d) ANALYSIS.md updated with the final 49-CWE table.

Output: Regenerated `benchmark/juliet/ast.json` and an updated `benchmark/juliet/ANALYSIS.md` with 8 new rows and updated 49-CWE total.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-ast-cwes-domainspecific-expansion/23-CONTEXT.md
@.planning/phases/23-ast-cwes-domainspecific-expansion/23-RESEARCH.md
@.planning/phases/23-ast-cwes-domainspecific-expansion/23-01-SUMMARY.md
@.planning/phases/23-ast-cwes-domainspecific-expansion/23-02-SUMMARY.md
@benchmark/juliet/ANALYSIS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-run Juliet benchmark and regenerate ast.json</name>
  <files>benchmark/juliet/ast.json</files>
  <read_first>
    - benchmark/juliet/ANALYSIS.md (existing structure — find documented regeneration command)
    - benchmark/juliet/ast.json (head only — confirm JSON shape and field names like cwe_id, file_path)
    - .planning/phases/23-ast-cwes-domainspecific-expansion/23-RESEARCH.md (Validation Architecture section)
  </read_first>
  <behavior>
    - `benchmark/juliet/ast.json` is regenerated by running the existing sc2sbom Juliet benchmark driver (the same one Phase 18/21/22 used).
    - The regenerated `ast.json` contains entries for at least one of CWE-114, 272, 284, 479, 785 (the CWEs with Juliet TPs per RESEARCH.md Juliet table).
    - The crate test suite passes after regeneration.
  </behavior>
  <action>
    1. Locate the existing Juliet benchmark driver. Read `benchmark/juliet/ANALYSIS.md` to find the documented regeneration command. Per Phase 18/21/22 pattern, candidates (in priority order):
       a. The exact command documented in `benchmark/juliet/ANALYSIS.md` (preferred — preserves consistency with prior phases).
       b. `bash benchmark/juliet/regenerate.sh` if such a script exists.
       c. `cargo run --features internal --release --bin juliet_benchmark` if a dedicated binary exists.
       d. `cargo run --features internal --release -- --sarif-output benchmark/juliet/ast.json example_target_repos/juliet-test-suite-c/testcases` as a last-resort fallback.

    2. Run the regeneration command. The Juliet corpus has thousands of files; allow up to 30 minutes (1800000 ms) via the bash timeout parameter.

    3. After regeneration, verify `benchmark/juliet/ast.json` exists, is non-empty, and contains at least one entry with `cwe_id` in {114, 272, 284, 479, 785}.

    4. Run `cargo test --features internal -p radeis_sc2sbom` as a sanity check that nothing in the source tree changed unexpectedly during the run.
  </action>
  <verify>
    <automated>test -s benchmark/juliet/ast.json && grep -cE '"cwe_?id"\s*:\s*"?(114|272|284|479|785)"?' benchmark/juliet/ast.json | awk '{ if ($1 >= 1) print "OK"; else { print "NO_NEW_CWE_FOUND"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `benchmark/juliet/ast.json` exists and is non-empty (`test -s benchmark/juliet/ast.json`)
    - `grep -cE '"cwe_?id"\s*:\s*"?(114|272|284|479|785)"?' benchmark/juliet/ast.json` returns at least 1
    - `cargo test --features internal -p radeis_sc2sbom` exits 0
  </acceptance_criteria>
  <done>
    Regenerated `ast.json` includes at least one Juliet-tractable Phase 23 CWE finding (114, 272, 284, 479, or 785).
  </done>
</task>

<task type="auto">
  <name>Task 2: Update benchmark/juliet/ANALYSIS.md with 8 new CWE rows and 49-CWE total</name>
  <files>benchmark/juliet/ANALYSIS.md</files>
  <read_first>
    - benchmark/juliet/ANALYSIS.md (entire file — must match existing per-CWE row format, table structure, and section headings)
    - benchmark/juliet/ast.json (sample lines — derive per-CWE TP/FP counts using file-level oracle)
    - .planning/phases/23-ast-cwes-domainspecific-expansion/23-CONTEXT.md (D-11 synthetic-TP rule)
    - .planning/phases/23-ast-cwes-domainspecific-expansion/23-RESEARCH.md (Juliet Corpus Coverage Facts table — expected outcomes)
  </read_first>
  <behavior>
    - `benchmark/juliet/ANALYSIS.md` contains 8 new per-CWE rows (CWE-114, 272, 284, 427, 479, 591, 762, 785) using the same column format as existing rows.
    - For CWE-427/591/762 (no Juliet TPs per D-11), the row explicitly notes "0 TPs on Juliet (acceptable per D-11); ≥1 TP via synthetic unit test" and cites the covering unit test.
    - For CWE-114/272/284/479/785, the row contains TP and FP counts derived from `ast.json` against the file-level oracle.
    - FP% for each Juliet-covered CWE is ≤40%. If FP% exceeds 40%, an `### FP Gate Violations` subsection documents the violation — do NOT silently accept.
    - The total CWE count is updated from the prior value to 49.
    - A `### Regression Check (existing 41 CWEs)` subsection (or equivalent) documents that prior-CWE TP counts did not decrease.
  </behavior>
  <action>
    1. Compute per-CWE TP/FP counts using the file-level oracle: for each finding with `cwe_id == N`, the file_path is a TP if it contains `CWE{N}_`, otherwise it is a FP. Follow whatever exact formula the existing ANALYSIS.md uses (read the file for the oracle definition; if absent, document the rule used).

    2. Append exactly 8 new rows to the per-CWE TP/FP table. Match the existing column format (likely `| CWE | TPs | FPs | FP% | Notes |`). Use these contents:

       - **CWE-114** — TPs: <computed>, FPs: <computed>, FP%: <computed>; Notes: "AnyCall on LoadLibraryA/W/ExA/ExW (Phase 23)."
       - **CWE-272** — TPs: <computed>, FPs: <computed>, FP%: <computed>; Notes: "AnyCall on CreateProcessAsUserA/W (Phase 23)."
       - **CWE-284** — TPs: <computed>, FPs: <computed>, FP%: <computed>; Notes: "ArgAtIndex(4, GENERIC_ALL) on CreateDesktopA/W (Phase 23)."
       - **CWE-427** — TPs: 0, FPs: 0, FP%: N/A; Notes: "Juliet uses PUTENV macro not direct putenv(); 0 TPs acceptable per D-11. Unit test TP: phase_23_cwe427_setenv_fires."
       - **CWE-479** — TPs: <computed>, FPs: <computed>, FP%: <computed>; Notes: "Two-pass signal()+helper non-reentrant detection (Phase 23)."
       - **CWE-591** — TPs: 0, FPs: 0, FP%: N/A; Notes: "Juliet uses malloc() not VirtualAlloc(); 0 TPs acceptable per D-11. Unit test TP: phase_23_cwe591_virtualalloc_without_virtuallock_fires."
       - **CWE-762** — TPs: 0, FPs: 0, FP%: N/A; Notes: "Juliet .cpp files with `namespace` trigger has_error() → lexical fallback; 0 TPs acceptable per D-11. Synthetic fixture: tests/fixtures/c/cwe762_delete_bad.c; unit test TP: phase_23_cwe762_delete_after_calloc_fires."
       - **CWE-785** — TPs: <computed>, FPs: <computed>, FP%: <computed>; Notes: "AnyCall on PathAppendA/W, realpath, _fullpath (Phase 23)."

    3. Update any "Total CWEs covered" / "Coverage: N CWEs" / "N CWE rules" header values in the document to **49**.

    4. Add a `### Phase 23 Coverage` subsection (mirroring the style of any existing `### Phase 21 Coverage` / `### Phase 22 Coverage` subsection) containing this text:

       "Phase 23 adds 8 domain-specific CWEs (114, 272, 284, 427, 479, 591, 762, 785). CWE-427, 591, and 762 have 0 Juliet TPs by design (corpus pattern mismatch — PUTENV macro, malloc-not-VirtualAlloc, namespace parse-error respectively) and are validated via unit tests per D-11. CWE-114, 272, 284, 479, and 785 are validated against Juliet ground truth with FP% ≤40%."

    5. Add a `### Regression Check (existing 41 CWEs)` subsection — or extend any existing regression-check section — containing a one-line statement per prior phase confirming that Phase 18/21/22 CWE TP counts did not decrease versus the pre-Phase-23 ANALYSIS.md baseline. List each prior-phase CWE that lost ≥1 TP as a regression (if any). If no regressions, state "No regression: all 41 prior CWE TP counts unchanged or improved."

    6. If any of CWE-114, 272, 284, 479, 785 shows FP% > 40%, add an `### FP Gate Violations` subsection listing each violating CWE, its FP%, and a hypothesis (e.g., "FP source: dangerous call appearing in `_good_` variant test files"). Do NOT silently update the table without flagging.
  </action>
  <verify>
    <automated>grep -cE 'CWE-?114|CWE-?272|CWE-?284|CWE-?427|CWE-?479|CWE-?591|CWE-?762|CWE-?785' benchmark/juliet/ANALYSIS.md | awk '{ if ($1 >= 8) print "OK"; else { print "MISSING_CWE_ROWS"; exit 1 } }' && grep -cE '\b49\b' benchmark/juliet/ANALYSIS.md | awk '{ if ($1 >= 1) print "TOTAL_OK"; else { print "TOTAL_MISSING"; exit 1 } }'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'CWE-114\|CWE114' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -c 'CWE-272\|CWE272' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -c 'CWE-284\|CWE284' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -c 'CWE-427\|CWE427' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -c 'CWE-479\|CWE479' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -c 'CWE-591\|CWE591' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -c 'CWE-762\|CWE762' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -c 'CWE-785\|CWE785' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -cE '\b49\b' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -c 'Phase 23' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -c 'D-11\|acceptable per D-11' benchmark/juliet/ANALYSIS.md` returns at least 1
    - `grep -c 'Regression Check\|no regression\|No regression' benchmark/juliet/ANALYSIS.md` returns at least 1
  </acceptance_criteria>
  <done>
    ANALYSIS.md has 8 new per-CWE rows, the total CWE count is 49, Phase 23 coverage subsection added, and regression check documented.
  </done>
</task>

</tasks>

<verification>
- `benchmark/juliet/ast.json` regenerated and contains Phase 23 CWE findings
- `benchmark/juliet/ANALYSIS.md` has 8 new rows + 49 total + Phase 23 subsection + regression check
- `cargo test --features internal -p radeis_sc2sbom` exits 0
- No FP gate violations remain unflagged (any >40% FP% has a dedicated subsection)
</verification>

<success_criteria>
- Juliet benchmark re-run with 49-CWE rule set
- ANALYSIS.md updated to reflect final 49-CWE coverage
- All 8 Phase 23 CWEs documented with TP/FP rows or synthetic-TP notes
- Regression check confirms no prior-CWE degradation
</success_criteria>

<output>
After completion, create `.planning/phases/23-ast-cwes-domainspecific-expansion/23-03-SUMMARY.md`
</output>
