---
phase: 14-cppcheck-integration
plan: 03
type: execute
wave: 1
depends_on: [01]
files_modified:
  - src/cli.rs
autonomous: true
requirements: [CPPCHECK-04]
must_haves:
  truths:
    - "Args struct exposes a --cppcheck-path CLI flag of type Option<PathBuf>"
    - "The flag is gated behind #[cfg(feature = \"internal\")] like other internal flags"
    - "The public (non-internal) build still compiles without exposing the flag"
  artifacts:
    - path: "src/cli.rs"
      provides: "cppcheck_path field on Args"
      contains: "cppcheck_path"
  key_links:
    - from: "src/cli.rs Args struct"
      to: "PathBuf"
      via: "Option<PathBuf>"
      pattern: "cppcheck_path:\\s*Option<PathBuf>"
---

<objective>
Add the `--cppcheck-path` CLI flag (D-15) to `Args` in `src/cli.rs`. This plan is intentionally narrow and isolated — `cli.rs` is touched by no other Phase 14 plan, allowing parallel execution with Plan 02 in Wave 1.

Purpose: Implements CPPCHECK-04 — provides the path override that Plan 04's `run_cppcheck_scanner` and Plan 05's `main.rs` integration consume.
Output: New `cppcheck_path: Option<PathBuf>` field on `Args`, gated behind `#[cfg(feature = "internal")]`, parsed by clap as `--cppcheck-path <PATH>`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-cppcheck-integration/14-CONTEXT.md
@.planning/phases/14-cppcheck-integration/14-RESEARCH.md
@.planning/phases/14-cppcheck-integration/14-PATTERNS.md
@src/cli.rs

<interfaces>
<!-- Existing internal-feature-gated CLI flag pattern (src/cli.rs:121-124) -->
```rust
/// Enable vulnerability checking (requires network connection)
#[cfg(feature = "internal")]
#[arg(long, action = ArgAction::Set, default_value_t = false)]
pub check_vulnerabilities: bool,
```

<!-- Existing Option<PathBuf> CLI flag pattern (src/cli.rs:268-271, supplier_config) -->
```rust
/// Path to custom BSW module config (YAML). ...
#[arg(long)]
pub supplier_config: Option<PathBuf>,
```

<!-- Args struct opens at src/cli.rs:100 ("pub struct Args {"), closes after line 271 -->
```rust
pub struct Args {
    /* ... many fields ... */
    pub supplier_config: Option<PathBuf>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --cppcheck-path flag to Args (gated behind internal feature)</name>
  <files>src/cli.rs</files>
  <read_first>
    - src/cli.rs (entire file — at least lines 1-30 for imports, 100-130 for Args opening + first internal-gated flag, 260-275 for the `supplier_config` field and the closing `}` of Args)
    - .planning/phases/14-cppcheck-integration/14-CONTEXT.md (D-15)
    - .planning/phases/14-cppcheck-integration/14-PATTERNS.md (cli.rs section)
  </read_first>
  <action>
  In `src/cli.rs`:

  1. Verify `use std::path::PathBuf;` is already imported at the top of the file. If it is not, add it. (It should already exist because `supplier_config: Option<PathBuf>` already compiles.)

  2. Find the `supplier_config: Option<PathBuf>,` field (around line 271). Immediately AFTER that field, but still inside `pub struct Args { ... }` (i.e., before the closing `}` of the struct), insert the new field exactly as follows:

  ```rust
      /// Path to cppcheck binary. When provided, uses this binary instead of PATH lookup. (v1.0.17)
      #[cfg(feature = "internal")]
      #[arg(long)]
      pub cppcheck_path: Option<PathBuf>,
  ```

  Match the indentation of `supplier_config` (4 spaces inside the struct).

  Constraints:
  - Use `#[cfg(feature = "internal")]` exactly — this matches every other internal-only flag (D-15, RESEARCH.md cli.rs section).
  - Use `#[arg(long)]` only — clap will derive `--cppcheck-path` from the field name (kebab-case conversion). Do NOT add `value_name` or `default_value_t`.
  - Type is `Option<PathBuf>`, not `Option<String>` — matches `supplier_config` and `bsw_config`.
  - Field name must be `cppcheck_path` (snake_case). Clap turns it into `--cppcheck-path` (kebab-case) automatically.

  Do NOT remove or modify any other field. Do NOT change `supplier_config`. Do NOT touch `main.rs` (that is Plan 05).
  </action>
  <verify>
    <automated>cargo build --features internal 2>&1 | tee /tmp/14-03-build.log && cargo build 2>&1 | tee -a /tmp/14-03-build.log; ! grep -E "^error" /tmp/14-03-build.log</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "pub cppcheck_path:\\s*Option<PathBuf>" src/cli.rs` returns exactly 1 line
    - `grep -B2 "pub cppcheck_path" src/cli.rs | grep -c "#\\[cfg(feature = \"internal\")\\]"` returns at least 1 (the cfg gate is present immediately above the field)
    - `grep -B1 "pub cppcheck_path" src/cli.rs | grep -c "#\\[arg(long)\\]"` returns at least 1 (the arg attribute is present)
    - `cargo build --features internal` exits 0
    - `cargo build` (default features, no `internal`) also exits 0 — proves the cfg gate works
    - `cargo run --features internal -- --help 2>&1 | grep -c -- "--cppcheck-path"` returns at least 1 (clap exposes the flag in help output)
  </acceptance_criteria>
  <done>
  `--cppcheck-path <PATH>` is parseable when built with `--features internal` and absent (cfg-gated out) in the default build. No other code paths are affected; Plans 04 and 05 will consume `args.cppcheck_path`.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User CLI input → Args parser | The `--cppcheck-path` value is supplied by the local user invoking sc2sbom; treated as untrusted by default but only used in Plan 04 via `Command::new(path)`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-06 | Elevation of privilege | `--cppcheck-path` arbitrary binary execution | accept | The flag is gated behind `#[cfg(feature = "internal")]` (D-15). Internal builds are produced for trusted CI/dev workflows; the binary is invoked only with `--xml --xml-version=2 --enable=warning,style,security <dir>` (Plan 04). The user already has shell-level execution rights on their own machine, so the flag adds no new privilege boundary. |
| T-14-07 | Tampering | Argument injection via path string | accept | `Command::new(path).args(["--xml", ...])` (Plan 04) passes the path as `program` and other args as discrete arguments — no shell, no string concatenation. Argument injection is structurally impossible. |
</threat_model>

<verification>
1. `cargo build --features internal` exits 0
2. `cargo build` (no internal feature) exits 0
3. `cargo run --features internal -- --help` shows `--cppcheck-path <CPPCHECK_PATH>` in usage output
4. `cargo run -- --help` (no internal) does NOT show `--cppcheck-path` (cfg gate verified)

ASVS L1 V5 input validation: PathBuf is used for type safety; no string interpolation into shell.
</verification>

<success_criteria>
- `cppcheck_path: Option<PathBuf>` field on `Args`, gated by `#[cfg(feature = "internal")]`
- Both `cargo build` and `cargo build --features internal` succeed
- `--cppcheck-path` appears in help output only under the internal feature
</success_criteria>

<output>
After completion, create `.planning/phases/14-cppcheck-integration/14-03-SUMMARY.md`
</output>
