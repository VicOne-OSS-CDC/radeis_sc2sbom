---
phase: 21
plan: 03
type: execute
wave: 3
depends_on: [21-01, 21-02]
files_modified:
  - benchmark/juliet/ANALYSIS.md
  - benchmark/juliet/ast.json
autonomous: false
requirements: [CWEXP-01]
tags: [benchmark, juliet, validation, cwe]

must_haves:
  truths:
    - "D-15, D-16: benchmark/juliet/ANALYSIS.md contains new per-CWE TP/FP rows for the 12 new CWEs (121, 126, 328, 338, 369, 426, 467, 526, 535, 676, 680, 780)"
    - "D-15: benchmark/juliet/ast.json has been regenerated from the post-Phase-21 scanner"
    - "ROADMAP success criterion 1: each new CWE has at least one TP recorded (TP source noted: juliet, or synthetic-fixture for CWEs not in Juliet corpus)"
    - "ROADMAP success criterion 2: FP% for each new CWE is at most 35% on the file-level oracle (or documented exception with rationale)"
    - "ROADMAP success criterion 3: no regression on the existing 13 CWEs; AST TPs and FPs for CWE-78, 119, 120, 122, 125, 134, 190, 242, 295, 319, 327, 377, 732 within plus-or-minus 5% of the pre-Phase-21 ANALYSIS.md baseline (or documented rationale for any drift)"
    - "Human reviewer confirms ANALYSIS.md values are accurate and CWE-369/676 corpus gaps documented (Juliet uses runtime divisors / cin; synthetic fixtures used for TP)"
  artifacts:
    - path: "benchmark/juliet/ANALYSIS.md"
      provides: "Updated per-CWE TP/FP table with 12 new rows; commentary on CWE-369 and CWE-676 corpus mismatches; regression check vs Phase 18 baseline"
      contains: "CWE-121"
    - path: "benchmark/juliet/ast.json"
      provides: "Regenerated raw findings JSON used by the oracle to compute the table"
      contains: "cwe_id"
  key_links:
    - from: "benchmark/juliet/ast.json"
      to: "benchmark/juliet/ANALYSIS.md per-CWE rows"
      via: "Oracle script tallies findings per CWE and per Juliet CWE directory"
      pattern: "CWE-(121|126|328|338|369|426|467|526|535|676|680|780)"
---

# Plan Overview

Re-run the AST scanner against the full Juliet C/C++ corpus, regenerate `benchmark/juliet/ast.json`, and update `benchmark/juliet/ANALYSIS.md` with per-CWE TP/FP rows for the 12 new CWEs added in Phase 21. Confirm the ROADMAP Phase 21 success criteria 1, 2, 3, and 4.

Purpose: ROADMAP Phase 21 success criterion 4 explicitly requires this benchmark file update. Without it, the requirement is unmet regardless of code correctness. Plan 03 is the final gate; it surfaces any rules whose FP% exceeds 35% or whose TP count is 0, so the team can adjust function lists or defer rules before phase close.

Output: An updated ANALYSIS.md with new rows, a regenerated ast.json, and a checkpoint where the user confirms the numbers and approves the Phase 21 close.

# Execution Context

- workflow: $HOME/.claude/get-shit-done/workflows/execute-plan.md
- summary template: $HOME/.claude/get-shit-done/templates/summary.md

# Context Files

- .planning/PROJECT.md
- .planning/ROADMAP.md
- .planning/STATE.md
- .planning/phases/21-ast-cwes-anycall-argpattern-expansion/21-CONTEXT.md
- .planning/phases/21-ast-cwes-anycall-argpattern-expansion/21-RESEARCH.md
- .planning/phases/21-ast-cwes-anycall-argpattern-expansion/21-01-SUMMARY.md
- .planning/phases/21-ast-cwes-anycall-argpattern-expansion/21-02-SUMMARY.md
- benchmark/juliet/ANALYSIS.md
- src/vulnerability/ast_scanner.rs

# Tasks

## Task 1 (type=auto): Re-run AST scanner on full Juliet corpus and regenerate benchmark/juliet/ast.json

Files modified: benchmark/juliet/ast.json

Read first:
- benchmark/juliet/ANALYSIS.md (full file; already contains the Phase 18 baseline for the existing 13 CWEs; the table format and oracle methodology section guide the update)
- .planning/phases/18-ast-scanner-core-and-benchmark/18-03-PLAN.md (the original benchmark generation plan; find the exact CLI invocation used to produce ast.json and mirror it)
- .planning/phases/21-ast-cwes-anycall-argpattern-expansion/21-RESEARCH.md section "Juliet Corpus Coverage Map" (per-CWE expected Juliet directory + corpus-gap notes for CWE-369/676)

Action:

1. Locate the Juliet corpus path. The existing ANALYSIS.md header says: `/Users/amean_lin/Desktop/GitRepos/radeis_sc2sbom/example_target_repos/juliet-test-suite-c`. Verify it still exists with `ls /Users/amean_lin/Desktop/GitRepos/radeis_sc2sbom/example_target_repos/juliet-test-suite-c | head -5`. If absent, STOP and report to the user; the Juliet corpus is required.

2. Find the existing benchmark runner script or CLI invocation. Check for:
   - `benchmark/juliet/run.sh` or similar (`ls benchmark/juliet/*.sh`)
   - `juliet-run.sh` mentioned in 21-RESEARCH.md (`find . -name juliet-run.sh -type f | head`)
   - The 18-03 plan or SUMMARY for the original command.

   If a runner script exists, invoke it. If not, derive the invocation from the SUMMARY; typically `cargo run --release --features internal -- --sast <juliet_root> --format sarif --output benchmark/juliet/ast.json` or a variant that writes the AST-only findings JSON. Adapt to whatever 18-03 actually used.

3. Build the release binary first to keep benchmark runtime manageable:
   `cargo build --release --features internal 2>&1 | tail -5`

4. Run the scanner on the Juliet corpus, redirecting output (or using the `--output` flag) to `benchmark/juliet/ast.json`. The corpus is approximately 100k files; expect 5 to 15 minutes. Use `nice` if desired. If a separate flag emits only AST-source findings, prefer that; otherwise filter post-run.

5. Validate the JSON output:
   `jq '.[] | .cwe_id' benchmark/juliet/ast.json | sort -u | head -30`
   The output must include the new CWE IDs 121, 126, 328, 338, 369, 426, 467, 526, 535, 676, 680, 780. Some may be 0-TP per the corpus-gap notes; verify presence vs absence reflects expectations from 21-RESEARCH.md.

6. Commit: `bench(21-03): regenerate ast.json from post-Phase-21 scanner against Juliet corpus`

Verify (automated): `cd /Users/amean_lin/Desktop/GitRepos/radeis_sc2sbom && test -f benchmark/juliet/ast.json && jq -r '.[] | .cwe_id' benchmark/juliet/ast.json | sort -u | tr '\n' ',' && wc -l benchmark/juliet/ast.json`

Acceptance criteria:
- `test -f benchmark/juliet/ast.json` exits 0
- `stat -f '%m' benchmark/juliet/ast.json` (macOS) or `stat -c '%Y'` (Linux) shows mtime within the last hour (regenerated, not stale)
- `jq '. | length' benchmark/juliet/ast.json` returns a positive integer (file is a valid JSON array)
- `jq -r '.[] | .cwe_id' benchmark/juliet/ast.json | sort -u | grep -c '^121$'` returns 1 (CWE-121 findings present)
- `jq -r '.[] | .cwe_id' benchmark/juliet/ast.json | sort -u | grep -E '^(126|338|426|467|526|535|680)$' | wc -l` returns at least 5
- `git status benchmark/juliet/ast.json` shows the file as modified or new

Done when: benchmark/juliet/ast.json regenerated from current ast_scanner.rs against the full Juliet C/C++ corpus; JSON is well-formed; new CWE IDs appear (with documented exceptions for corpus-gap CWEs); file committed.

## Task 2 (type=auto): Update benchmark/juliet/ANALYSIS.md with per-CWE TP/FP rows and regression check

Files modified: benchmark/juliet/ANALYSIS.md

Read first:
- benchmark/juliet/ANALYSIS.md (current full file; preserve the header, Oracle Method, and existing pre-Phase-21 rows; APPEND or UPDATE the per-CWE table and add a "Phase 21 Update" section)
- benchmark/juliet/ast.json (regenerated in Task 1)
- .planning/phases/21-ast-cwes-anycall-argpattern-expansion/21-RESEARCH.md section "Juliet Corpus Coverage Map" (expected Juliet directory per CWE and corpus-gap notes for CWE-369 variable divisor and CWE-676 cin operator)
- .planning/phases/21-ast-cwes-anycall-argpattern-expansion/21-CONTEXT.md (D-15, D-16)

Action:

1. Compute per-CWE TP/FP counts using the same file-level oracle methodology already described in ANALYSIS.md "Oracle Method" section. A finding in a file under `CWE{NNN}_*` directory counts as a TP if `cwe_id` matches NNN (or a family parent per the equivalence table in ANALYSIS.md); otherwise it counts as a FP. Re-use any existing oracle script; if none exists in-repo, write a one-shot script (or a `jq` + shell pipeline) and save it to `benchmark/juliet/oracle.sh` (or `benchmark/juliet/compute_table.sh`) for reproducibility.

   Minimum data needed per new CWE: TPs, FPs, and FP% = `FPs / (TPs + FPs) * 100`. Match the existing table column format exactly:

       | CWE-NNN | AST_TPs | AST_FPs | FP% | ... |

   Use `—` for cells where Lexical or cppcheck columns are not applicable (lexical scanner has no rule for the new CWEs after Phase 19 removal; cppcheck is removed in Phase 19).

2. Update the "Per-CWE TP/FP Table" in ANALYSIS.md by inserting rows for each of the 12 new CWEs (121, 126, 328, 338, 369, 426, 467, 526, 535, 676, 680, 780) in CWE-number order, alongside the existing rows.

3. Append a new section titled `## Phase 21 Update (2026-05-12)` with these subsections:

   a. **New per-CWE rows added.** List the 12 new CWEs with their TP, FP, and FP% values. Flag any CWE where FP% > 35% as "EXCEEDS GATE" with brief rationale (e.g., function list too broad; recommend tightening in a future phase). Flag any CWE where TP == 0 as "ZERO-TP NOTE" with the corpus-gap reason (e.g., "Juliet CWE-369 corpus uses runtime variable divisor `100/data`, not literal `/0`; AST rule fires only on literal `/0`. TP confirmed via inline unit test test_cwe_369_division_literal_zero, not via Juliet.").

   b. **Expected zero-TP CWEs (corpus mismatch, validated via unit tests):**
      - CWE-369: Juliet uses runtime variable divisor (`100 / data`); AST rule targets literal `/0`. Unit test `test_cwe_369_division_literal_zero` covers TP.
      - CWE-676: Juliet uses C++ `cin >>` operator (not a call_expression); AST rule targets `alloca` and `strtok` function calls. Unit test `test_cwe_676_dangerous_function_strtok` covers TP.
      - CWE-780: Juliet uses Windows `CryptEncrypt` without CRYPT_OAEP flag; AST rule targets OpenSSL `RSA_public_encrypt` (D-10). Unit test `test_cwe_780_rsa_no_oaep` covers TP.
      - CWE-328: Juliet uses Windows `CryptCreateHash` with `CALG_*` constants; AST rule TARGETS this. TP expected from Juliet.

   c. **Regression check on existing 13 CWEs.** Compare the regenerated ast.json TP and FP counts for CWE-78, 119, 120, 122, 125, 134, 190, 242, 295, 319, 327, 377, 732 against the pre-Phase-21 ANALYSIS.md baseline. Acceptable drift: plus-or-minus 5% on TPs and FPs. Document any cell that drifted more than plus-or-minus 5% with a one-line rationale (e.g., "CWE-78 TPs +4.0% due to new test fixture files in corpus; no rule change").

   d. **ROADMAP success criteria status table:**

       | Criterion | Status | Evidence |
       | #1 at-least-one TP per new CWE | met or met-via-unit-test (list which CWE used which) | rows above |
       | #2 FP% at-most 35% per new CWE | met or partial (list exceptions) | rows above |
       | #3 no regression on 13 existing CWEs | met or drift documented | regression subsection |
       | #4 ANALYSIS.md updated | met | this document |

4. Update the "Raw Finding Counts" section's `ast` row with the new total findings count from the regenerated ast.json (`jq '. | length' benchmark/juliet/ast.json`).

5. Commit: `docs(21-03): update benchmark/juliet/ANALYSIS.md with Phase 21 per-CWE TP/FP rows`

Per D-15, D-16. ROADMAP Phase 21 success criteria 1 through 4.

Verify (automated): `cd /Users/amean_lin/Desktop/GitRepos/radeis_sc2sbom && grep -c '^| CWE-121 ' benchmark/juliet/ANALYSIS.md && grep -c '^| CWE-126 ' benchmark/juliet/ANALYSIS.md && grep -c '^| CWE-535 ' benchmark/juliet/ANALYSIS.md && grep -c '^| CWE-780 ' benchmark/juliet/ANALYSIS.md && grep -c '## Phase 21 Update' benchmark/juliet/ANALYSIS.md`

Acceptance criteria:
- `grep -c '^| CWE-121 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-126 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-328 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-338 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-369 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-426 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-467 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-526 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-535 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-676 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-680 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '^| CWE-780 ' benchmark/juliet/ANALYSIS.md` returns at least 1
- `grep -c '## Phase 21 Update' benchmark/juliet/ANALYSIS.md` returns exactly 1
- `grep -cE 'ROADMAP success criteria|success criteria status' benchmark/juliet/ANALYSIS.md` returns at least 1
- `git status benchmark/juliet/ANALYSIS.md` shows the file as modified

Done when: All 12 new CWE rows present in the per-CWE table; "Phase 21 Update" section explains corpus gaps and regression status; ROADMAP success criteria status table is populated; file committed.

## Task 3 (type=checkpoint:human-verify, gate=blocking): Human review of ANALYSIS.md numbers and Phase 21 close

What built:
- benchmark/juliet/ast.json regenerated from post-Phase-21 scanner (Task 1)
- benchmark/juliet/ANALYSIS.md updated with 12 new per-CWE rows + Phase 21 Update section (Task 2)
- ROADMAP success criteria 1-4 status documented in the Phase 21 Update section

How to verify:
1. Open `benchmark/juliet/ANALYSIS.md` and locate the "Phase 21 Update (2026-05-12)" section.
2. Confirm the 12 new CWE rows present in the per-CWE TP/FP table (121, 126, 328, 338, 369, 426, 467, 526, 535, 676, 680, 780).
3. Review the FP% column for each new CWE:
   - All values at or below 35%: criterion 2 met.
   - Any value above 35%: confirm the rationale explains why and whether the rule needs tightening in a future phase. Accept or request remediation.
4. Review the TP column for each new CWE:
   - All values >= 1: criterion 1 met.
   - Any value == 0: confirm the corpus-gap note correctly explains why and which unit test covers the TP case. CWE-369, CWE-676, and potentially CWE-780 are expected zero-TP via Juliet per RESEARCH.md.
5. Review the regression subsection. Confirm no existing-13-CWE drift exceeds plus-or-minus 5%, or that any drift has a documented rationale.
6. Approve to close Phase 21, OR list specific issues to fix.

Resume signal: Type "approved" to mark Phase 21 complete, or describe specific changes needed.

# Threat Model

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Juliet corpus filesystem to AST scanner | Untrusted-by-convention source files; scanner consumes them as test input only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | T (Tampering) | ANALYSIS.md row values incorrect because oracle script bug or stale ast.json | mitigate | Task 3 checkpoint puts a human in the loop; the oracle script (when saved to benchmark/juliet/oracle.sh) is reproducible and re-runnable. ast.json regeneration is the explicit Task 1 step. |
| T-21-03-02 | I (Information Disclosure) | Benchmark output may include absolute paths from the scanning machine | accept | The benchmark filesystem path is already in the existing ANALYSIS.md header; this is intentional for reproducibility. No PII or secrets in C source files. |
| T-21-03-03 | D (Denial of Service) | Full Juliet scan takes 5-15 minutes; may time out under default 2 min Bash timeout | mitigate | Task 1 explicitly notes the expected runtime; executor should pass `timeout: 1200000` (20 minutes) to the Bash tool when invoking the scanner. |
| T-21-03-04 | E (Elevation of Privilege) | n/a; benchmark is read-only against corpus | accept | No new attack surface introduced by re-running the scanner or updating a markdown file. |
| T-21-03-05 | R (Repudiation) | Without committing oracle script, ANALYSIS.md numbers cannot be reproduced | mitigate | Task 2 instructs the executor to save the oracle script to benchmark/juliet/oracle.sh (or compute_table.sh) so future runs can validate. |

# Verification

After all three tasks:
- `test -f benchmark/juliet/ast.json` exits 0 and mtime is recent
- `grep -c '## Phase 21 Update' benchmark/juliet/ANALYSIS.md` returns 1
- All 12 new CWE rows present in the table (grep counts above)
- ROADMAP success criteria status table populated
- Human reviewer approves the close (Task 3 checkpoint resolved with "approved")
- `git log --oneline -5` shows both the ast.json regeneration commit and the ANALYSIS.md update commit

# Success Criteria

1. benchmark/juliet/ast.json regenerated from the post-Phase-21 scanner (D-15).
2. benchmark/juliet/ANALYSIS.md has 12 new per-CWE rows in the table (D-15, D-16).
3. ROADMAP Phase 21 success criterion 1 (at least one TP per new CWE) verified for each CWE — either from Juliet or via the noted unit test (zero-TP rationale documented for CWE-369/676/780).
4. ROADMAP Phase 21 success criterion 2 (FP% at most 35%) verified for each CWE or exception documented.
5. ROADMAP Phase 21 success criterion 3 (no regression on 13 existing CWEs) verified within plus-or-minus 5% drift or rationale provided.
6. ROADMAP Phase 21 success criterion 4 (ANALYSIS.md updated) met.
7. Human reviewer approves the Phase 21 close.

# Output

After completion, create `.planning/phases/21-ast-cwes-anycall-argpattern-expansion/21-03-SUMMARY.md` per `~/.claude/get-shit-done/templates/summary.md`. Include:
- ast.json regeneration confirmation with new total findings count
- Per-CWE TP/FP summary table (the 12 new CWEs)
- ROADMAP success criteria status (1-4) with evidence pointers
- Any CWE that exceeded the 35% FP gate with recommended follow-up (defer to a future plan)
- Phase 21 close declaration; hand-off to Phase 22 (structural-pattern expansion)
