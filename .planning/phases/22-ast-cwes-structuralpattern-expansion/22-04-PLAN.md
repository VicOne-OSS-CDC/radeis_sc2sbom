---
phase: 22
plan: 04
type: execute
wave: 4
depends_on: [22-03]
files_modified:
  - benchmark/juliet/ANALYSIS.md
  - benchmark/juliet/ast.json
autonomous: false
requirements:
  - CWEXP-02
tags:
  - benchmark
  - juliet
  - validation
  - documentation

must_haves:
  truths:
    - "benchmark/juliet/ANALYSIS.md contains a per-CWE TP/FP row for each of the 15 Phase 22 CWEs (256, 398, 478, 480, 481, 482, 483, 484, 562, 570, 571, 587, 617, 674, 835)"
    - "benchmark/juliet/ast.json is refreshed with the post-Phase-22 scanner run output"
    - "Each Phase 22 CWE meets the ROADMAP floor: ≥1 TP on Juliet (per D-13)"
    - "Per-CWE FP% > 40% is documented (not blocked) per D-11"
    - "AUTOSAR fixture regression check completed and recorded (D-15) — no existing CWE finding count changed"
  artifacts:
    - path: "benchmark/juliet/ANALYSIS.md"
      provides: "Updated per-CWE TP/FP table with 15 new rows"
      contains: "CWE-256"
    - path: "benchmark/juliet/ast.json"
      provides: "Fresh AST scanner Juliet output for Phase 22 baseline"
  key_links:
    - from: "benchmark/juliet/ANALYSIS.md"
      to: "all 15 Phase 22 CWE rows"
      via: "appended table rows"
      pattern: "CWE-(256|398|478|480|481|482|483|484|562|570|571|587|617|674|835)"
---

<objective>
Validate Phase 22 against the Juliet C test suite and update benchmark/juliet/ANALYSIS.md per D-14, and confirm no regression on the AUTOSAR fixture per D-15.

Purpose: The Phase 22 ROADMAP success criteria require (1) each new CWE produces ≥1 TP on Juliet, (2) FP% documented, (3) no regression on existing 26 CWEs, (4) ANALYSIS.md updated. This plan executes the benchmark and records results — it is the verification gate, not new rule code.
Output: Updated ANALYSIS.md, refreshed ast.json, AUTOSAR regression evidence, human checkpoint approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/22-ast-cwes-structuralpattern-expansion/22-CONTEXT.md
@.planning/phases/22-ast-cwes-structuralpattern-expansion/22-RESEARCH.md
@.planning/phases/22-ast-cwes-structuralpattern-expansion/22-PATTERNS.md
@.planning/phases/22-ast-cwes-structuralpattern-expansion/22-03-PLAN.md
@benchmark/juliet/ANALYSIS.md

<interfaces>
The Juliet benchmark harness exists from Phase 18 (BENCH-01). The exact invocation must be discovered from the existing harness — check:
- benchmark/juliet/README.md (if exists)
- benchmark/juliet/run.sh or similar
- Phase 18 SUMMARYs in .planning/phases/18-ast-scanner-core-and-benchmark/

Existing ANALYSIS.md table header (Pattern from 22-PATTERNS.md):
```
| CWE | AST TPs | AST FPs | AST FP% | Lexical TPs | Lexical FPs | Lexical FP% | cppcheck TPs | cppcheck FPs | cppcheck FP% |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run Juliet benchmark and capture per-CWE TP/FP counts for 15 Phase 22 CWEs</name>
  <files>benchmark/juliet/ast.json, tmp/phase22_juliet_counts.md</files>
  <read_first>
    - benchmark/juliet/ANALYSIS.md (full file — to learn existing row format, existing per-CWE columns, and the file-level oracle definition used)
    - benchmark/juliet/README.md (if exists)
    - benchmark/juliet/ — list directory to find run script / harness binary / makefile target
    - .planning/phases/18-ast-scanner-core-and-benchmark/*-SUMMARY.md (Phase 18 benchmark run commands; the harness exists from BENCH-01)
  </read_first>
  <action>
    1. Discover the Juliet benchmark invocation from the existing harness. Possible paths to check (in order):
       - `benchmark/juliet/run.sh` or `benchmark/juliet/run_benchmark.sh`
       - Make targets: `grep -E 'juliet' Makefile 2>/dev/null` or top-level `make help`
       - Cargo binary: `cargo run --features internal --bin sc2sbom -- --benchmark-juliet <path>` (if Phase 18 added a CLI flag)
       - Phase 18 SUMMARY notes
       If no harness script exists, the run is a direct invocation of sc2sbom on the Juliet C/CWE corpus root and the resulting SARIF/JSON output is the AST scanner findings.

    2. Run the benchmark with the post-Phase-22 binary. Capture findings to `benchmark/juliet/ast.json` (overwrite existing).

    3. Compute per-CWE file-level TP/FP using the Juliet oracle: scanner CWE_id matches the Juliet directory CWE family (e.g., a CWE-478 finding in a file under `testcases/CWE478_*/...` is a TP; the same finding in a file under any other CWE directory is an FP).
       - Acceptable approach: a small awk/python/jq script reading ast.json and the Juliet directory tree.
       - Output a temp counts table at `tmp/phase22_juliet_counts.md` for use in Task 2:
       ```
       | CWE | TP files | FP files | Total flagged | FP% |
       | CWE-256 | N | N | N | NN% |
       ...
       | CWE-835 | N | N | N | NN% |
       ```

    4. Per D-13 floor: each of the 15 CWEs MUST have TP ≥ 1. If ANY CWE has 0 TPs:
       - Inspect the Juliet directory for that CWE and identify the AST shape the rule misses.
       - File a tracked issue in the SUMMARY for follow-on; do not block Phase 22 if the rule fires correctly on a synthetic fixture (unit tests already prove the rule works). The D-11 policy applies: ship and document.
       - Mark the CWE row in `tmp/phase22_juliet_counts.md` with `0 TP — see SUMMARY for known gap`.

    Note on assumptions A1–A4 in RESEARCH.md: if any tree-sitter node-kind assumption was wrong (e.g., `for_statement` doesn't use a field named `"condition"`), the unit tests in Plans 01–03 would have caught it. If a Juliet TP count is unexpectedly 0 despite the unit test passing, the cause is likely a Juliet-specific code shape not covered by the synthetic test, NOT a tree-sitter API gap.
  </action>
  <verify>
    <automated>cd /Users/amean_lin/Desktop/GitRepos/radeis_sc2sbom && test -f benchmark/juliet/ast.json && test -f tmp/phase22_juliet_counts.md && grep -cE '^\| CWE-(256|398|478|480|481|482|483|484|562|570|571|587|617|674|835) ' tmp/phase22_juliet_counts.md</automated>
  </verify>
  <acceptance_criteria>
    - File `benchmark/juliet/ast.json` exists and mtime is newer than `src/vulnerability/ast_scanner.rs`
    - File `tmp/phase22_juliet_counts.md` exists and contains exactly 15 rows matching the Phase 22 CWE list (verify with `grep -cE '^\| CWE-(256|398|478|480|481|482|483|484|562|570|571|587|617|674|835) ' tmp/phase22_juliet_counts.md` == 15)
    - Each row has a numeric TP count (regex: `\| [0-9]+ \|`)
    - Any CWE with 0 TPs is explicitly listed in tmp/phase22_juliet_counts.md with a comment `— see SUMMARY for known gap`
  </acceptance_criteria>
  <done>Juliet benchmark run completed, ast.json refreshed, per-CWE counts captured to scratch file.</done>
</task>

<task type="auto">
  <name>Task 2: Update benchmark/juliet/ANALYSIS.md — append 15 new per-CWE rows + Phase 22 section</name>
  <files>benchmark/juliet/ANALYSIS.md</files>
  <read_first>
    - benchmark/juliet/ANALYSIS.md (full file — to find the existing per-CWE TP/FP table and the correct insertion point)
    - tmp/phase22_juliet_counts.md (from Task 1)
    - .planning/phases/22-ast-cwes-structuralpattern-expansion/22-PATTERNS.md (Per-CWE row format)
  </read_first>
  <action>
    1. Insert 15 new rows into the existing per-CWE TP/FP table, in CWE id order, maintaining the existing column order:
    ```
    | CWE-256 | <TP> | <FP> | <FP%>% | 0 | 0 | — | 0 | 0 | — |
    | CWE-398 | <TP> | <FP> | <FP%>% | 0 | 0 | — | 96 | 138158 | 99.9% |
    | CWE-478 | <TP> | <FP> | <FP%>% | 0 | 0 | — | <existing or 0> | ... |
    ...
    | CWE-835 | <TP> | <FP> | <FP%>% | 0 | 0 | — | <existing or 0> | ... |
    ```
    For cppcheck columns: copy existing CWE values from earlier sections of ANALYSIS.md if present (CWE-398 has 96/138158 per RESEARCH.md). For CWEs without prior cppcheck data, use `0 | 0 | —`. For Lexical columns: 0/0/— for all 15 (no lexical rules for these CWEs).

    2. Add a new section heading "## Phase 22 Notes" near the end of ANALYSIS.md describing:
       - D-05 chosen approach: body-check for CWE-835 (rationale: AUTOSAR FP reduction)
       - D-06 known gap: CWE-570/571 literal-only rule achieves ~12% TP on Juliet by design (variable patterns explicitly deferred)
       - D-07: CWE-398 implemented with 4 sub-rules per RESEARCH.md recommendation
       - D-11: list any CWEs exceeding 40% FP (documented, not blocked)
       - D-13 floor confirmation: list any CWE with 0 Juliet TPs (known gaps) — should be empty or short
       - D-15: AUTOSAR fixture regression check result (recorded in Task 3)

    3. Make NO changes to any existing row or any pre-Phase-22 text — additions only.
  </action>
  <verify>
    <automated>cd /Users/amean_lin/Desktop/GitRepos/radeis_sc2sbom && grep -cE '^\| CWE-(256|398|478|480|481|482|483|484|562|570|571|587|617|674|835) \|' benchmark/juliet/ANALYSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^\| CWE-(256|398|478|480|481|482|483|484|562|570|571|587|617|674|835) \|' benchmark/juliet/ANALYSIS.md` returns ≥ 15
    - `grep -c '## Phase 22 Notes' benchmark/juliet/ANALYSIS.md` == 1
    - `grep -c 'D-05' benchmark/juliet/ANALYSIS.md` ≥ 1 (rationale documented)
    - `grep -c 'D-06' benchmark/juliet/ANALYSIS.md` ≥ 1 (CWE-570/571 gap documented)
    - `grep -c 'D-15' benchmark/juliet/ANALYSIS.md` ≥ 1 (AUTOSAR regression note present)
    - No existing pre-Phase-22 rows altered: `git diff benchmark/juliet/ANALYSIS.md` shows only insertions, no modifications, no deletions (verify via `git diff --stat` and inspect)
  </acceptance_criteria>
  <done>ANALYSIS.md updated with 15 new rows + Phase 22 Notes section; existing content untouched.</done>
</task>

<task type="auto">
  <name>Task 3: AUTOSAR fixture regression check (D-15) — capture pre/post counts, record evidence</name>
  <files>tmp/phase22_autosar_regression.md, benchmark/juliet/ANALYSIS.md</files>
  <read_first>
    - benchmark/juliet/ANALYSIS.md (Phase 22 Notes section from Task 2 — to know where to append D-15 result)
    - tests/ — find AUTOSAR fixture path (typically `tests/fixtures/AUTOSAR_SampleProject_S32K144/` or referenced in run_ast_scanner tests)
    - .planning/STATE.md (Phase 19 / Phase 20 summaries — for baseline AUTOSAR finding counts if recorded)
  </read_first>
  <action>
    1. Run the scanner on the AUTOSAR_SampleProject_S32K144 fixture (or whichever AUTOSAR fixture the existing test suite/Phase 19/20 used). Capture finding counts BY CWE.

    2. Compare against the pre-Phase-22 baseline:
       - Baseline source: prior phase ANALYSIS.md AUTOSAR row, or re-run on `master` / on the prior commit if no recorded baseline exists.
       - For each existing pre-Phase-22 CWE (78, 119, 120, 122, 125, 134, 190, 242, 295, 319, 327, 377, 732), confirm the finding count is UNCHANGED.

    3. Write `tmp/phase22_autosar_regression.md`:
       ```
       | CWE | Pre-Phase-22 count | Post-Phase-22 count | Δ |
       | CWE-78 | N | N | 0 |
       ...
       | CWE-617 (new) | — | N | +N |
       ```

    4. Append a one-line summary to the "## Phase 22 Notes" section in ANALYSIS.md:
       - If Δ == 0 for all existing CWEs: `D-15 regression check: PASS — all 13 pre-existing AUTOSAR CWE counts unchanged. New Phase 22 CWEs added <N> findings (additive).`
       - If Δ ≠ 0 for any existing CWE: `D-15 regression check: FAIL — CWE-XXX count changed from N to M.` (and the plan must NOT pass the next checkpoint; investigate Pitfall 4 (CWE-617 assert interaction) or other rule overlap before re-running.)
  </action>
  <verify>
    <automated>cd /Users/amean_lin/Desktop/GitRepos/radeis_sc2sbom && test -f tmp/phase22_autosar_regression.md && grep -E 'D-15 regression check: (PASS|FAIL)' benchmark/juliet/ANALYSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - File `tmp/phase22_autosar_regression.md` exists with one row per pre-Phase-22 CWE
    - `grep -c 'D-15 regression check: PASS' benchmark/juliet/ANALYSIS.md` == 1 (regression check passed)
    - If regression check FAILED, the next checkpoint (Task 4) MUST block — do not mark this task done until investigation completes and either (a) the failure is fixed or (b) it is explicitly accepted by the user
    - For each pre-existing CWE row in the regression file, Δ column equals 0
  </acceptance_criteria>
  <done>AUTOSAR regression evidence captured; D-15 status recorded in ANALYSIS.md.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human checkpoint — verify Phase 22 benchmark results acceptable to ship</name>
  <what-built>
    Phase 22 added 15 new structural CWE detection rules (256, 398, 478, 480, 481, 482, 483, 484, 562, 570, 571, 587, 617, 674, 835) to `src/vulnerability/ast_scanner.rs`. All 15 have synthetic unit tests passing. Juliet benchmark has run; per-CWE TP/FP recorded in `benchmark/juliet/ANALYSIS.md`. AUTOSAR fixture regression check has been performed and recorded.
  </what-built>
  <how-to-verify>
    1. Open `benchmark/juliet/ANALYSIS.md` and locate the "## Phase 22 Notes" section. Confirm:
       - All 15 new CWE rows present with TP/FP counts
       - D-15 regression check shows PASS
       - D-13 floor (≥1 TP per CWE) — note any CWE that shows 0 TPs (per RESEARCH.md, CWE-570/571 may be at the floor due to D-06 narrowing; this is expected)
       - D-11 documented FPs — any CWE >40% FP is recorded but NOT blocked

    2. Open `tmp/phase22_autosar_regression.md` and confirm Δ == 0 for every pre-existing CWE row.

    3. Decide:
       - If results look acceptable (≥1 TP per CWE, AUTOSAR Δ all 0): approve.
       - If a CWE has 0 TPs and you want it investigated before merge: respond with "fix CWE-XXX" and a new plan will be created in `--gaps` mode.
       - If FP% on some CWE is uncomfortably high and you want stricter filters: respond with "tighten CWE-XXX" and provide direction.
  </how-to-verify>
  <resume-signal>
    Type one of:
    - "approved" — Phase 22 ships as documented
    - "fix CWE-XXX: <reason>" — gap closure plan will be created
    - "tighten CWE-XXX: <new constraint>" — follow-on adjustment plan will be created
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Juliet C corpus → scanner | The Juliet test files are public, well-known, not actively attacker-controlled. Standard parse-failure fallback applies (AST-04). |
| ANALYSIS.md edits → committed docs | Plain markdown edit, no code execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-11 | D (DoS) | Juliet corpus scan (thousands of files) | accept | Benchmark is a developer-only operation; runtime is bounded by tree-sitter parse speed; no production runtime impact |
| T-22-12 | T (Tampering) | ANALYSIS.md numbers | mitigate | Numbers come directly from ast.json + Juliet directory oracle; reproducible by re-running Task 1; checkpointed in git history |
| T-22-13 | I (Info disclosure) | benchmark output paths | accept | Paths point only into the local Juliet corpus, not user code |
</threat_model>

<verification>
- `benchmark/juliet/ANALYSIS.md` contains all 15 new CWE rows with numeric TP/FP values
- `benchmark/juliet/ast.json` mtime is newer than the last `src/vulnerability/ast_scanner.rs` edit
- `tmp/phase22_autosar_regression.md` shows Δ == 0 for all pre-Phase-22 CWEs (D-15)
- The "## Phase 22 Notes" section documents D-05, D-06, D-11, D-13, D-15 outcomes
- Human checkpoint approved
</verification>

<success_criteria>
- ROADMAP Phase 22 success criterion 1: "≥1 TP on Juliet for each new CWE" — recorded and confirmed per row
- ROADMAP Phase 22 success criterion 2: "FP% documented for each new CWE" — recorded in ANALYSIS.md row
- ROADMAP Phase 22 success criterion 3: "No regression on existing 26 CWEs" — D-15 regression check PASS recorded
- ROADMAP Phase 22 success criterion 4: "benchmark/juliet/ANALYSIS.md updated" — done with 15 new rows
- User approves the benchmark output at the checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/22-ast-cwes-structuralpattern-expansion/22-04-SUMMARY.md` documenting:
- Final per-CWE TP/FP table (copy of the 15 new ANALYSIS.md rows)
- D-15 regression result (PASS/FAIL + evidence)
- Any known gaps to track (e.g., CWE-570/571 below 50% TP per D-06)
- Cumulative Phase 22 outcome: 13 → 41 CWEs (vs. planned 26 → 41)
- Recommendation for Phase 23 entry (or follow-on gap closure if user requested any)
</output>
