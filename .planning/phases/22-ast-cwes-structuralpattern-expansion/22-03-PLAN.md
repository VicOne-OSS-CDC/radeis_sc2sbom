---
phase: 22
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - tests/vulnerability_tests/ast_scanner_tests.rs
  - src/vulnerability/ast_scanner.rs
autonomous: true
requirements:
  - CWEXP-02
tags:
  - rust
  - tree-sitter
  - sast
  - cwe

must_haves:
  truths:
    - "Unit tests exist for CWE-617, 674, 256, 835, 398 (each TP + at least one TN where applicable) and pass after implementation"
    - "AST_CWE_RULES table contains an AnyCall entry for assert (CWE-617)"
    - "check_self_recursion, check_plaintext_password, check_infinite_loop, check_poor_code_quality are private functions in ast_scanner.rs"
    - "apply_ast_rules calls the 4 new check_* functions; CWE-617 fires via existing visit_node AnyCall path"
    - "Full cargo test --features internal passes; cumulative 15 new CWE detections implemented"
  artifacts:
    - path: "tests/vulnerability_tests/ast_scanner_tests.rs"
      provides: "Unit tests for CWE-617, 674, 256, 835, 398"
      contains: "test_cwe674_direct_self_recursion"
    - path: "src/vulnerability/ast_scanner.rs"
      provides: "4 new structural visitor functions + assert entry in AST_CWE_RULES"
      contains: "fn check_self_recursion"
  key_links:
    - from: "src/vulnerability/ast_scanner.rs:AST_CWE_RULES"
      to: "ArgCheck::AnyCall for assert (CWE-617)"
      via: "table entry consumed by visit_node call_expression loop"
      pattern: "cwe_id: 617"
    - from: "src/vulnerability/ast_scanner.rs:check_self_recursion"
      to: "extracted function name from function_declarator → walk body for matching call_expression"
      via: "D-04 intra-function self-call detection"
      pattern: "fn check_self_recursion"
---

<objective>
Implement Group C of Phase 22 — call-based, identifier-heuristic, and loop-shape CWEs.

Purpose: Complete the 15-CWE structural expansion with CWE-617 (assert via AnyCall), CWE-674 (direct self-recursion per D-03/D-04), CWE-256 (plaintext password by identifier name per D-08/D-10), CWE-835 (infinite loop, body-check approach per D-05 recommendation), and CWE-398 (poor code quality, four sub-rules per D-07 recommendation).
Output: 4 new private `check_*` functions + 1 new AST_CWE_RULES entry; 5 unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-ast-cwes-structuralpattern-expansion/22-CONTEXT.md
@.planning/phases/22-ast-cwes-structuralpattern-expansion/22-RESEARCH.md
@.planning/phases/22-ast-cwes-structuralpattern-expansion/22-PATTERNS.md
@.planning/phases/22-ast-cwes-structuralpattern-expansion/22-02-PLAN.md
@src/vulnerability/ast_scanner.rs

<interfaces>
AST_CWE_RULES table (ast_scanner.rs lines 50-78) — existing static array of AstCweRule. AnyCall pattern:
```rust
AstCweRule { cwe_id: 190, functions: &["malloc", "calloc", "realloc"], arg_check: ArgCheck::AnyCall },
```

CWE-617 entry to add (Pattern G in 22-PATTERNS.md):
```rust
AstCweRule { cwe_id: 617, functions: &["assert"], arg_check: ArgCheck::AnyCall },
```

`find_enclosing_function` reused for CWE-674.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add 6 failing tests for CWE-617, 674, 256, 835, 398</name>
  <files>tests/vulnerability_tests/ast_scanner_tests.rs</files>
  <read_first>
    - tests/vulnerability_tests/ast_scanner_tests.rs (current state after Plan 02)
    - .planning/phases/22-ast-cwes-structuralpattern-expansion/22-RESEARCH.md (CWE-617, CWE-674, CWE-256, CWE-835, CWE-398 sections — exact bad-fixture snippets)
  </read_first>
  <behavior>
    - test_cwe617_assert_call: bad C: `extern void assert(int); void f(int x){assert(x > 0);}` → emits CWE-617
    - test_cwe674_direct_self_recursion: bad C: `void helperBad(){helperBad();}` → emits CWE-674
    - test_cwe674_no_recursion_no_finding: good C: `void g(){} void f(){g();}` → NO CWE-674
    - test_cwe256_password_string_literal: bad C: `void f(){char *password = "hunter2";}` → emits CWE-256
    - test_cwe256_pwd_uppercase: bad C: `void f(){char *PWD = "x";}` → emits CWE-256 (case-insensitive)
    - test_cwe256_non_password_var_no_finding: good C: `void f(){char *username = "alice";}` → NO CWE-256
    - test_cwe835_while_one_no_break: bad C: `void f(){while(1){int x=1;}}` → emits CWE-835
    - test_cwe835_while_one_with_break_no_finding: good C: `void f(){while(1){break;}}` → NO CWE-835 (body-check D-05)
    - test_cwe398_bare_number_statement: bad C: `void f(){5;}` → emits CWE-398
    - test_cwe398_self_assignment: bad C: `void f(int x){x = x;}` → emits CWE-398
    - test_cwe398_discarded_arithmetic: bad C: `void f(int a, int b){a + b;}` → emits CWE-398
  </behavior>
  <action>
    Append the listed tests using `setup_one_file` and the existing assertion pattern. Tests MUST fail at this point.
  </action>
  <verify>
    <automated>cd /Users/amean_lin/Desktop/GitRepos/radeis_sc2sbom && cargo test --features internal vulnerability_tests::ast_scanner_tests::test_cwe617 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File contains `fn test_cwe617_assert_call`, `fn test_cwe674_direct_self_recursion`, `fn test_cwe674_no_recursion_no_finding`, `fn test_cwe256_password_string_literal`, `fn test_cwe256_pwd_uppercase`, `fn test_cwe256_non_password_var_no_finding`, `fn test_cwe835_while_one_no_break`, `fn test_cwe835_while_one_with_break_no_finding`, `fn test_cwe398_bare_number_statement`, `fn test_cwe398_self_assignment`, `fn test_cwe398_discarded_arithmetic` (grep each, count == 1)
    - Tests compile and fail (red state expected)
  </acceptance_criteria>
  <done>11 new tests added and red.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CWE-617 to AST_CWE_RULES + implement check_self_recursion (CWE-674) + check_plaintext_password (CWE-256)</name>
  <files>src/vulnerability/ast_scanner.rs</files>
  <read_first>
    - src/vulnerability/ast_scanner.rs (current state after Plan 02 — to see AST_CWE_RULES table layout and existing helpers)
    - .planning/phases/22-ast-cwes-structuralpattern-expansion/22-RESEARCH.md (CWE-617 section + Pitfall 4; CWE-674 section + Code Examples; CWE-256 section)
    - .planning/phases/22-ast-cwes-structuralpattern-expansion/22-PATTERNS.md (Pattern G for CWE-617; Pattern E for CWE-674)
  </read_first>
  <action>
    1. Add to `AST_CWE_RULES` static array (place after the CWE-732 SetSecurityDescriptorDacl entry, before the closing `]`):
    ```rust
    AstCweRule { cwe_id: 617, functions: &["assert"], arg_check: ArgCheck::AnyCall },
    ```
    No other code change needed for CWE-617 — the existing `visit_node` AnyCall path handles it.

    2. Add `fn check_self_recursion(root, src, path, component_name, component_ecosystem, findings)` + recursive helper `fn check_self_calls(node, src, fn_name, path, component_name, component_ecosystem, findings)` following the Code Example in 22-RESEARCH.md verbatim. Key logic:
       - At root level: iterate `function_definition` children of root via fresh cursor.
       - For each function_definition: extract the function name by walking `child_by_field_name("declarator")` → drill through `pointer_declarator` / `parenthesized_declarator` layers until reaching `function_declarator`, then `function_declarator.child_by_field_name("declarator")` → identifier. Write a small helper `fn extract_function_name(fn_node: Node, src: &[u8]) -> Option<String>`.
       - Walk function body for `call_expression` whose `function` field text equals the extracted name. Push CWE-674 ONCE per function_definition (use a `bool fired` flag inside check_self_calls or check before recursing).
       - SastFinding line = call_expression line (start_position().row + 1).

    3. Add `fn check_plaintext_password(node, src, path, component_name, component_ecosystem, findings)`:
       - If `node.kind() == "declaration"`:
         - Walk declarator subtree to find the identifier and the initializer:
           - For an `init_declarator` child: declarator side = `child_by_field_name("declarator")` (drill through pointer_declarator → identifier); value side = `child_by_field_name("value")`.
         - Extract identifier text via utf8_text. Lowercase the name. If it contains ANY of `"password"`, `"passwd"`, `"pwd"`, `"secret"` (substring match) → check initializer:
           - If initializer `.kind() == "string_literal"` → push SastFinding { cwe_id: 256, line of declaration }
       - Recurse via fresh-cursor pattern (do NOT short-circuit on declaration match — files may contain nested declarations).

    4. Add to `apply_ast_rules` after Plan 02 calls:
    ```rust
    check_self_recursion(root, src, path, component_name, component_ecosystem, &mut findings);
    check_plaintext_password(root, src, path, component_name, component_ecosystem, &mut findings);
    ```

    (No call needed for CWE-617 — visit_node fires it from AST_CWE_RULES.)
  </action>
  <verify>
    <automated>cd /Users/amean_lin/Desktop/GitRepos/radeis_sc2sbom && cargo test --features internal vulnerability_tests::ast_scanner_tests::test_cwe617 vulnerability_tests::ast_scanner_tests::test_cwe674 vulnerability_tests::ast_scanner_tests::test_cwe256 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'cwe_id: 617' src/vulnerability/ast_scanner.rs` ≥ 1
    - `grep -c 'functions: &\["assert"\]' src/vulnerability/ast_scanner.rs` == 1
    - `grep -c 'fn check_self_recursion' src/vulnerability/ast_scanner.rs` == 1
    - `grep -c 'fn extract_function_name' src/vulnerability/ast_scanner.rs` == 1
    - `grep -c 'fn check_plaintext_password' src/vulnerability/ast_scanner.rs` == 1
    - `grep -c 'cwe_id: 674' src/vulnerability/ast_scanner.rs` ≥ 1
    - `grep -c 'cwe_id: 256' src/vulnerability/ast_scanner.rs` ≥ 1
    - Heuristic substrings present: `grep -E '"password"|"passwd"|"pwd"|"secret"' src/vulnerability/ast_scanner.rs` returns ≥ 4 matches
    - `check_self_recursion(root` and `check_plaintext_password(root` each appear once in apply_ast_rules
    - Tests pass: `cargo test --features internal vulnerability_tests::ast_scanner_tests::test_cwe617_assert_call vulnerability_tests::ast_scanner_tests::test_cwe674_direct_self_recursion vulnerability_tests::ast_scanner_tests::test_cwe674_no_recursion_no_finding vulnerability_tests::ast_scanner_tests::test_cwe256_password_string_literal vulnerability_tests::ast_scanner_tests::test_cwe256_pwd_uppercase vulnerability_tests::ast_scanner_tests::test_cwe256_non_password_var_no_finding` exits 0
  </acceptance_criteria>
  <done>CWE-617, 674, 256 tests green; 11 of 15 Phase 22 CWEs now implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement check_infinite_loop (CWE-835) + check_poor_code_quality (CWE-398)</name>
  <files>src/vulnerability/ast_scanner.rs</files>
  <read_first>
    - src/vulnerability/ast_scanner.rs (current state after Task 2)
    - .planning/phases/22-ast-cwes-structuralpattern-expansion/22-RESEARCH.md (CWE-835 section with body-check D-05 recommendation; CWE-398 section with 4 sub-rules per D-07)
    - .planning/phases/22-ast-cwes-structuralpattern-expansion/22-CONTEXT.md (D-05, D-07)
  </read_first>
  <action>
    1. Add `fn check_infinite_loop(node, src, path, component_name, component_ecosystem, findings)` using BODY-CHECK approach (D-05 recommendation — fewer AUTOSAR FPs):
       - If `node.kind() == "while_statement"`:
         - Get `cond = child_by_field_name("condition")`. Unwrap parenthesized_expression layer.
         - If `cond.kind() == "number_literal"` and text (after suffix strip) parses to non-zero integer (`while(1)`):
           - Get `body = child_by_field_name("body")`.
           - If body subtree contains NO `break_statement`, NO `return_statement`, NO `goto_statement`, AND NO `call_expression` whose function field text is `"exit"` or `"abort"` → push CWE-835.
       - If `node.kind() == "for_statement"`:
         - Check if condition is empty (`child_by_field_name("condition")` returns None, OR an empty/missing node). If empty `for(;;)` → same body-check as above.
       - Helper: `fn body_has_escape(node: Node, src: &[u8]) -> bool` — recurses; returns true if any descendant is `break_statement`, `return_statement`, `goto_statement`, or `call_expression` to exit/abort. Fresh cursor per level.
       - Recurse via fresh-cursor pattern at outer level.

    2. Add `fn check_poor_code_quality(node, src, path, component_name, component_ecosystem, findings)` with FOUR sub-rules per D-07 recommendation:
       - If `node.kind() == "expression_statement"`:
         - Take the first named child `inner`.
         - Sub-rule 1: `inner.kind() == "number_literal"` → push CWE-398 (covers Juliet "five" family).
         - Sub-rule 2: `inner.kind() == "binary_expression"` AND operator text is one of `"+", "-", "*", "/", "%", "|", "&", "^"` → push CWE-398 (covers "addition" family — discarded arithmetic).
         - Sub-rule 3: `inner.kind() == "binary_expression"` AND operator text is `"=="` → push CWE-398 (covers "equals" family overlap; intentionally overlaps with CWE-482 per RESEARCH.md note — same defect).
         - Sub-rule 4: `inner.kind() == "assignment_expression"` AND LHS identifier text equals RHS identifier text (self-assignment): get `left = child_by_field_name("left")`, `right = child_by_field_name("right")`; both kind `identifier` and same `utf8_text` → push CWE-398.
       - Recurse via fresh-cursor pattern. Take care: multiple sub-rules may match the same statement — fire AT MOST ONCE per expression_statement (early return after first push).

    3. Add to `apply_ast_rules` after Task 2 calls:
    ```rust
    check_infinite_loop(root, src, path, component_name, component_ecosystem, &mut findings);
    check_poor_code_quality(root, src, path, component_name, component_ecosystem, &mut findings);
    ```

    SastFinding construction MUST follow Pattern B verbatim.
  </action>
  <verify>
    <automated>cd /Users/amean_lin/Desktop/GitRepos/radeis_sc2sbom && cargo test --features internal 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'fn check_infinite_loop' src/vulnerability/ast_scanner.rs` == 1
    - `grep -c 'fn check_poor_code_quality' src/vulnerability/ast_scanner.rs` == 1
    - `grep -c 'fn body_has_escape' src/vulnerability/ast_scanner.rs` == 1
    - `grep -c 'cwe_id: 835' src/vulnerability/ast_scanner.rs` ≥ 1
    - `grep -c 'cwe_id: 398' src/vulnerability/ast_scanner.rs` ≥ 1
    - `check_infinite_loop(root` and `check_poor_code_quality(root` each appear once in apply_ast_rules
    - `grep -E '"break_statement"|"return_statement"|"goto_statement"' src/vulnerability/ast_scanner.rs` returns ≥ 3 matches (body-check D-05)
    - All Phase 22 tests pass: `cargo test --features internal vulnerability_tests::ast_scanner_tests::test_cwe835 vulnerability_tests::ast_scanner_tests::test_cwe398` exits 0
    - Body-check works: `test_cwe835_while_one_with_break_no_finding` (good variant) is GREEN
    - Full suite green: `cargo test --features internal 2>&1 | grep -E '^test result:'` shows `0 failed`
    - `cargo build --features internal --release 2>&1 | grep -Ec '^warning:'` does not increase vs. pre-Phase-22 baseline (no NEW warnings)
  </acceptance_criteria>
  <done>All 15 Phase 22 CWEs detected; full suite green; ready for benchmark in Plan 04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| C source file → tree-sitter parser | Same as Plans 01–02; parse failures fall back per AST-04. |
| Identifier text → substring match | Case-insensitive substring match on declarator text — bounded by file size, no panic risk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-07 | D (DoS) | check_self_recursion + body_has_escape recursion | mitigate | Fresh cursor per level; bounded by AST depth ≤ file size; no exponential paths |
| T-22-08 | T (Tampering) | function name extraction | mitigate | `utf8_text` returns Result; missing/invalid name → check_self_calls is simply not invoked for that function |
| T-22-09 | I (Info disclosure) | CWE-256 password identifier match | mitigate | Only the file path + line is reported in findings; the actual password string is NOT included in SastFinding output (consistent with existing scanner output format) |
| T-22-10 | E (Elevation) | n/a — read-only static analysis | accept | No execution of analyzed code; tool only parses + emits findings |
</threat_model>

<verification>
- 11 new CWE unit tests in ast_scanner_tests.rs all green (CWE-617, 674×2, 256×3, 835×2, 398×3)
- Plan 01 + Plan 02 tests remain green
- `cargo test --features internal` full suite green
- `cargo build --features internal --release` succeeds with no new warnings
- AST_CWE_RULES table has new CWE-617 entry; 4 new check_* functions added
</verification>

<success_criteria>
- All 15 Phase 22 CWEs (256, 478, 480, 481, 482, 483, 484, 562, 570, 571, 587, 617, 674, 835, 398) detect on synthetic TP fixtures with SastSource::Ast
- Body-check approach for CWE-835 confirmed by green TN test (`while(1){break;}` does NOT fire)
- Self-assignment detected for CWE-398 (`x = x`) without firing on assignment-like patterns inside conditions (those go to CWE-481)
- Per D-15: existing CWE detection counts unchanged on full suite (any non-CWE-22 test regression is a fail)
</success_criteria>

<output>
After completion, create `.planning/phases/22-ast-cwes-structuralpattern-expansion/22-03-SUMMARY.md` documenting:
- New functions added with line counts (cumulative across plans 01-03)
- Final list of AST_CWE_RULES entries (note CWE-617 added)
- Per-CWE TP results from unit tests (all 15 listed)
- Any divergences from research (e.g., tree-sitter-c condition field name surprises)
- Ready-for-benchmark statement
</output>
