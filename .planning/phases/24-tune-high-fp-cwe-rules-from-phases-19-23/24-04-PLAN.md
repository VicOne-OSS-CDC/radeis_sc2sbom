---
phase: 24-tune-high-fp-cwe-rules-from-phases-19-23
plan: 04
type: execute
wave: 4
depends_on: ["24-03"]
files_modified:
  - benchmark/juliet/ast.json
  - benchmark/juliet/ANALYSIS.md
autonomous: false
requirements: [TBD-24-D]
tags: [validation, oracle, juliet, autosar, regression]

must_haves:
  truths:
    - "benchmark/juliet/ast.json regenerated against the post-Phase-24 scanner build"
    - "oracle.sh executed successfully; per-CWE TP/FP rows fully regenerated in ANALYSIS.md (no in-place edits)"
    - "ANALYSIS.md contains a `## Phase 24 Notes` section listing: fixed CWEs with before/after FP%, removed CWE-256, residual >35% FP CWEs flagged as human-review items"
    - "Human reviewer approves the Juliet delta before AUTOSAR regression is run"
    - "AUTOSAR_SampleProject_S32K144 fixture finding counts on pre-existing CWEs (i.e., CWEs not modified in Phase 24) are unchanged"
  artifacts:
    - path: "benchmark/juliet/ast.json"
      provides: "Full Juliet AST scanner output after Phase 24 fixes"
    - path: "benchmark/juliet/ANALYSIS.md"
      provides: "Regenerated per-CWE TP/FP table + Phase 24 Notes section"
      contains:
        - "## Phase 24 Notes"
        - "CWE-256"
  key_links:
    - from: "src/vulnerability/ast_scanner.rs (post Plan 03)"
      to: "benchmark/juliet/ast.json"
      via: "scanner run over juliet-test-suite-c"
      pattern: "ast.json mtime > Plan 03 commit"
    - from: "benchmark/juliet/ast.json"
      to: "benchmark/juliet/ANALYSIS.md table"
      via: "oracle.sh"
      pattern: "Per-CWE table rows regenerated from ast.json"
---

<objective>
Wave 4: validation gate. Run the Juliet oracle against the Phase-24-tightened scanner, regenerate the per-CWE TP/FP table in `benchmark/juliet/ANALYSIS.md` from scratch (D-25), add a "Phase 24 Notes" section documenting what changed (D-26), then pause for human review of the Juliet delta (D-23). After human approval, run the AUTOSAR regression check on `AUTOSAR_SampleProject_S32K144` and confirm that pre-existing CWEs' finding counts are unchanged.

Purpose: This is the validation half of D-21 (implement first, validate once). It is the only plan whose output answers whether the 17 fixes actually moved FP% in the expected direction, and it produces the documentation that closes the phase per D-27.

Output:
- Regenerated `benchmark/juliet/ast.json`
- Fully regenerated per-CWE TP/FP table in `benchmark/juliet/ANALYSIS.md`
- `## Phase 24 Notes` section in ANALYSIS.md
- Human-verify checkpoint approving the Juliet delta
- AUTOSAR regression results confirming no regression on pre-existing CWEs
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-CONTEXT.md
@.planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-RESEARCH.md
@.planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-PATTERNS.md
@.planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-01-SUMMARY.md
@.planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-02-SUMMARY.md
@.planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-03-SUMMARY.md
@.planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-03-CWE587-INVESTIGATION.md
@benchmark/juliet/ANALYSIS.md
@benchmark/juliet/oracle.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: Regenerate benchmark/juliet/ast.json + run oracle.sh + regenerate ANALYSIS.md per-CWE table + write Phase 24 Notes</name>
  <files>benchmark/juliet/ast.json, benchmark/juliet/ANALYSIS.md</files>
  <read_first>
    - benchmark/juliet/oracle.sh (entire file — verifies invocation contract)
    - benchmark/juliet/ANALYSIS.md (read current per-CWE table to understand row format; do NOT edit in place — regenerate)
    - .planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-03-CWE587-INVESTIGATION.md (CWE-587 outcome — input to Phase 24 Notes)
    - .planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-01-SUMMARY.md, 24-02-SUMMARY.md, 24-03-SUMMARY.md (before/after deltas)
    - benchmark/juliet/ANALYSIS.md §Phase 21 EXCEEDS GATE rationale + §Phase 22 D-11 + §Phase 23 FP Gate Violations (prior root-cause notes — needed for Phase 24 Notes "before" column)
  </read_first>
  <action>
    1) **Verify Juliet corpus available.**
       ```bash
       test -d example_target_repos/juliet-test-suite-c && echo OK || echo MISSING
       ```
       If MISSING, stop and surface to user — oracle cannot run without the corpus.

    2) **Regenerate `benchmark/juliet/ast.json`.** Use the existing scanner invocation that produced the prior ast.json. The exact command is documented in benchmark/juliet/README.md or in the Phase 21/22/23 SUMMARYs — re-read those if needed. The general shape (per RESEARCH.md and ROADMAP Phase 21 notes) is:
       ```bash
       cargo run --release --features internal -- \
           --target example_target_repos/juliet-test-suite-c \
           --sarif-output /tmp/juliet.sarif \
           --json-output benchmark/juliet/ast.json
       ```
       Use whatever invocation Phase 21/22/23 used (they're reproducible per CONTEXT.md "specifics"). If the JSON output flag has a different name in current CLI, read `src/main.rs` / `src/cli.rs` to find it. The output MUST be at `benchmark/juliet/ast.json` for oracle.sh to consume.

    3) **Run the oracle.** From repo root:
       ```bash
       ./benchmark/juliet/oracle.sh 2>&1 | tee /tmp/p24-oracle.out
       ```
       (oracle.sh auto-resolves paths per RESEARCH.md.) Capture the per-CWE TP/FP counts from stdout — the script prints them in a tabular form.

    4) **Regenerate the per-CWE TP/FP table in `benchmark/juliet/ANALYSIS.md`.** Replace the entire existing "Per-CWE" table with fresh rows from the oracle output. Do NOT edit individual rows in place — delete the old table and write a new one with all CWEs sourced from oracle output. The table column layout must match the existing schema in ANALYSIS.md (CWE ID, TP, FP, FP%, Status). If unclear, copy the current header row verbatim and refill the rows.

    5) **Append a `## Phase 24 Notes` section** at the end of ANALYSIS.md with this exact structure:
       ```markdown
       ## Phase 24 Notes

       ### Removed (D-03)
       - CWE-256 — removed from scanner (`check_plaintext_password` call deleted from `apply_ast_rules`). Reason: 100% FP, 0 TP on Juliet corpus; corpus uses Windows API patterns not matched by string-identifier heuristic. No replacement rule.

       ### Tightened (before → after FP%)
       For each of the 16 tightened CWEs (126, 338, 426, 467, 478, 480, 483, 535, 562, 570, 571, 587, 676, 680, 762, 780), one row:
       - CWE-<N> — <before>% → <after>% — D-<NN> applied: <one-line summary of the fix>

       ### Residual >35% FP (human-review items per D-24)
       Listed CWEs whose post-fix FP% is still above 35%. Each row:
       - CWE-<N> — post-fix FP%: <X>% — root cause hypothesis: <text> — recommended action: <accept | remove | follow-up phase>

       ### Coverage delta
       - Pre-Phase-24 coverage: 49 CWEs
       - Post-Phase-24 coverage: 48 CWEs (CWE-256 removed)
       ```

       Fill in actual numbers from the oracle output. For the "before" column, source values from the Phase 21/22/23 sections currently in ANALYSIS.md (or the Phase 24 target list in ROADMAP.md §Phase 24). For CWE-587, use the CWE-587 investigation file's recommended action.

    6) **Do not commit yet** — Task 2 (human checkpoint) follows. Stage changes for review.
  </action>
  <verify>
    <automated>test -f benchmark/juliet/ast.json && test -s benchmark/juliet/ast.json && grep -c '## Phase 24 Notes' benchmark/juliet/ANALYSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f benchmark/juliet/ast.json` exit 0 and `wc -l benchmark/juliet/ast.json` > 100 (non-trivial output)
    - `benchmark/juliet/ast.json` mtime is newer than the most recent commit on the Phase 24 branch (oracle ran after Plan 03 changes)
    - `grep -c '^## Phase 24 Notes' benchmark/juliet/ANALYSIS.md` outputs `1`
    - `grep -c 'CWE-256 — removed' benchmark/juliet/ANALYSIS.md` outputs `1`
    - `grep -cE 'CWE-(126|338|426|467|478|480|483|535|562|570|571|587|676|680|762|780)' benchmark/juliet/ANALYSIS.md` outputs at least `16` matches in the Phase 24 Notes section
    - The Per-CWE table in ANALYSIS.md has at least 48 data rows (the post-Phase-24 coverage)
    - No row in the Phase 24 Notes "Tightened" section contains placeholder text like `<before>` or `<after>` — all numbers filled in
  </acceptance_criteria>
  <done>ast.json regenerated; oracle ran; ANALYSIS.md fully regenerated with Phase 24 Notes section; numbers from real oracle output.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human review of Juliet oracle delta</name>
  <what-built>
    Plans 01–03 applied 17 CWE rule tightenings to `src/vulnerability/ast_scanner.rs`. Task 1 regenerated `benchmark/juliet/ast.json`, re-ran the oracle, fully regenerated the per-CWE TP/FP table in `benchmark/juliet/ANALYSIS.md`, and added a `## Phase 24 Notes` section documenting before/after FP%, the CWE-256 removal, and any residual >35% FP CWEs flagged for human-review (per D-24).
  </what-built>
  <how-to-verify>
    1. Open `benchmark/juliet/ANALYSIS.md` and read the new `## Phase 24 Notes` section.
    2. For each of the 17 target CWEs, confirm:
       - The before/after FP% direction is sane (after ≤ before, or the "Residual" subsection explains why not).
       - The Status column reflects the new TP/FP counts.
    3. Open `.planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-03-CWE587-INVESTIGATION.md` and confirm the CWE-587 outcome matches what Phase 24 Notes claims (either a code change with measurable FP drop, or a no-op with documented human-review status).
    4. Optionally re-run the oracle yourself:
       ```bash
       ./benchmark/juliet/oracle.sh 2>&1 | less
       ```
       and compare against the table.
    5. Confirm any CWE flagged as human-review (per D-24) has a documented disposition you accept (accept / remove in follow-up phase / accept with documented risk).
  </how-to-verify>
  <resume-signal>
    Type `approved` to proceed to AUTOSAR regression (Task 3). If issues are found, describe them — the executor will return to whichever Plan (01/02/03) needs revision before re-running this validation.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: AUTOSAR_SampleProject_S32K144 regression check</name>
  <files></files>
  <read_first>
    - .planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-CONTEXT.md (D-23, D-27)
    - benchmark/juliet/ANALYSIS.md (regenerated Phase 24 table — to know which CWEs were modified and therefore excluded from regression comparison)
    - Any prior AUTOSAR baseline SARIF if present (look for `benchmark/autosar/*.sarif` or similar; if absent, the regression is a direct run + count comparison against the most recent pre-Phase-24 commit)
  </read_first>
  <action>
    1) **Identify the AUTOSAR baseline.** Search for existing AUTOSAR baseline output:
       ```bash
       ls benchmark/autosar/ 2>/dev/null || ls example_target_repos/AUTOSAR_SampleProject_S32K144 2>/dev/null
       ```
       If a pre-Phase-24 baseline SARIF/JSON exists under `benchmark/autosar/`, use it for comparison. If none exists, run the scanner at the parent commit of the Phase 24 branch first to capture a baseline.

    2) **Run the scanner on AUTOSAR_SampleProject_S32K144 with the Phase-24-tightened build:**
       ```bash
       cargo run --release --features internal -- \
           --target example_target_repos/AUTOSAR_SampleProject_S32K144 \
           --sarif-output /tmp/p24-autosar.sarif
       ```
       (Adjust the flag names to match the current CLI.)

    3) **Compare finding counts per CWE between baseline and Phase-24 run.**
       Use a small python or jq script — exemplar:
       ```bash
       jq -r '.runs[0].results[] | .ruleId' /tmp/p24-autosar.sarif | sort | uniq -c | sort -k2 > /tmp/p24-autosar-counts.txt
       cat /tmp/p24-autosar-counts.txt
       ```
       (Adjust extraction if SARIF schema differs.)

    4) **Determine "pre-existing CWEs"** = the set of CWEs detected by the scanner that are NOT among the 17 modified CWEs in Phase 24 (126, 256, 338, 426, 467, 478, 480, 483, 535, 562, 570, 571, 587, 676, 680, 762, 780).

       For each pre-existing CWE, the Phase-24 finding count MUST equal the baseline count. For each modified CWE, the count is expected to differ (and the direction must match what ANALYSIS.md predicts).

    5) **Write the result into the ANALYSIS.md Phase 24 Notes section.** Append at the end of that section:
       ```markdown
       ### AUTOSAR regression (D-23)
       - Baseline: <commit hash or path>
       - Phase 24 run: /tmp/p24-autosar.sarif (or committed path)
       - Pre-existing CWE finding counts unchanged: <yes | no — list deltas>
       - Modified CWE finding counts: <list each: CWE-<N>: baseline=X, post=Y>
       ```

    6) If ANY pre-existing CWE shows a delta, halt and surface to user — this indicates an unintended regression that needs investigation before phase close.
  </action>
  <verify>
    <automated>grep -c '### AUTOSAR regression' benchmark/juliet/ANALYSIS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '### AUTOSAR regression' benchmark/juliet/ANALYSIS.md` outputs `1`
    - The section contains an explicit "Pre-existing CWE finding counts unchanged" line with either "yes" or a listed delta
    - If "no", the surfaced delta is documented for the user — phase does not auto-close
    - `cargo build --release --features internal -p radeis-sc2sbom` exits 0 (scanner builds in release mode for this regression run)
  </acceptance_criteria>
  <done>AUTOSAR regression run; delta documented in ANALYSIS.md; unintended regressions surfaced if any.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Juliet corpus | Read-only source files; no execution; tree-sitter parses without privilege |
| AUTOSAR fixture | Read-only source files; same constraint as Juliet |
| oracle.sh Python heredoc | Local execution in repo root; no network; no privilege escalation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-10 | Tampering | benchmark/juliet/ast.json | accept | Regenerated artifact; committed under version control; not used by production scanner |
| T-24-11 | Repudiation | ANALYSIS.md Phase 24 Notes | mitigate | Human-verify checkpoint (Task 2) requires explicit `approved` signal before AUTOSAR regression runs |
| T-24-12 | Information Disclosure | SARIF output paths under /tmp | accept | Local-only paths used for diff; no upload |
</threat_model>

<verification>
- benchmark/juliet/ast.json exists, non-empty, newer than Plan 03
- benchmark/juliet/ANALYSIS.md has `## Phase 24 Notes` section with all 17 CWEs accounted for (16 tightened + 1 removed) plus AUTOSAR regression sub-section
- Human-verify checkpoint received `approved` before AUTOSAR regression ran
- AUTOSAR pre-existing CWE counts unchanged (or deltas explicitly surfaced)
</verification>

<success_criteria>
- All 17 CWE outcomes documented in ANALYSIS.md (with before/after FP%)
- CWE-256 removal documented
- CWE-587 outcome from 24-03-CWE587-INVESTIGATION.md reflected
- Residual >35% FP items captured as human-review items per D-24
- AUTOSAR regression complete and clean
- Phase 24 close criteria (D-27) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/24-tune-high-fp-cwe-rules-from-phases-19-23/24-04-SUMMARY.md`.
</output>
