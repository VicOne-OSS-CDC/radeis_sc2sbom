---
phase: 10-internal-feature-gate
plan: 03
type: execute
wave: 3
depends_on: ["10-02"]
files_modified:
  - src/cli.rs
  - src/main.rs
  - src/formats/cyclonedx.rs
  - src/formats/spdx.rs
  - src/formats/console.rs
autonomous: true
requirements: [GATE-01, GATE-02]
must_haves:
  truths:
    - "Public binary --help output contains zero vulnerability/CWE/CVSS-related strings"
    - "All MinSeverity, VulnerabilityOutputMode, SbomMode enums are gated"
    - "All eight vuln-related Args fields are gated"
    - "All formatter vulnerability emission branches are gated"
    - "Public build (cargo build --release) compiles successfully"
    - "Internal build (cargo build --release --features internal) still compiles successfully"
  artifacts:
    - path: "src/cli.rs"
      provides: "Gated vuln Args fields and enums"
      contains: "cfg(feature = \"internal\")"
    - path: "src/formats/cyclonedx.rs"
      provides: "Gated vulnerability serialization"
      contains: "cfg(feature = \"internal\")"
    - path: "src/formats/spdx.rs"
      provides: "Gated vuln external refs"
      contains: "cfg(feature = \"internal\")"
    - path: "src/formats/console.rs"
      provides: "Gated vulnerability rendering"
      contains: "cfg(feature = \"internal\")"
  key_links:
    - from: "src/cli.rs Args"
      to: "vulnerability scanning block in main.rs"
      via: "gated Args fields only consumable when feature is on"
      pattern: "cfg\\(feature = \"internal\"\\).*\\n.*#\\[arg"
---

<objective>
Gate the CLI args and enums in src/cli.rs and the vulnerability serialization branches in all three formatters (cyclonedx.rs, spdx.rs, console.rs). After this plan the public build (cargo build --release) MUST compile cleanly and the public binary's --help output MUST contain zero vulnerability-related strings.

Purpose: Closes GATE-01 (public binary has no CVE scanning code) and GATE-02 (public binary has no NVD/CWE enrichment code) at the CLI surface and formatter surface. This is the final source-code gating plan.

Output: cargo build --release exits 0; the public binary --help output has no matches for vulner|cwe|cvss|cache-ttl.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-internal-feature-gate/10-CONTEXT.md
@.planning/phases/10-internal-feature-gate/10-RESEARCH.md
@.planning/phases/10-internal-feature-gate/10-PATTERNS.md
@.planning/phases/10-internal-feature-gate/10-02-SUMMARY.md

<interfaces>
src/cli.rs enums to gate (per RESEARCH lines 275-295):
- MinSeverity enum (lines ~29-50) and its impl block with to_level method
- VulnerabilityOutputMode enum (lines ~52-60)
- SbomMode enum (lines ~72-78)

src/cli.rs Args fields to gate (8 fields, lines ~118-159):
- check_vulnerabilities: bool
- min_severity: MinSeverity
- vulnerability_timeout: u64
- vulnerability_output: VulnerabilityOutputMode
- cache_ttl: u64
- clear_cache: bool
- max_vulns_per_severity: Option type
- sbom_mode: SbomMode

src/formats/cyclonedx.rs items to gate:
- use crate::cli::SbomMode (line 1 area)
- VulnerabilitySeverity import in models import block
- pub vulnerabilities field on CycloneDXDocument (~line 41)
- Supporting structs: CycloneDXVulnerability, CycloneDXVulnerabilitySource, CycloneDXVulnerabilityRating, CycloneDXVulnerabilityReference, CycloneDXVulnerabilityAffect (~lines 180-243)
- build_cyclonedx_vulnerabilities function (~lines 248-393)
- The mode parameter on convert_to_cyclonedx and the SbomMode-filtering block inside it

src/formats/spdx.rs items to gate:
- use crate::cli::SbomMode (line 1)
- VulnerabilitySeverity import (if present)
- The Add vulnerability references loop in create_external_refs (~lines 223-241)
- The SbomMode filtering block inside convert_to_spdx (~lines 558-562)
- The mode parameter on print_spdx_json, print_spdx_tag_value, convert_to_spdx, save_spdx_json, save_spdx_tag_value

src/formats/console.rs items to gate:
- use crate::cli::TreeStyle, VulnerabilityOutputMode — split: keep TreeStyle unconditional, gate VulnerabilityOutputMode
- use crate::models import — split: keep base imports unconditional, gate Vulnerability/VulnerabilitySeverity
- count_unique_cves function
- print_vulnerabilities_hierarchical function
- All code blocks referencing dep.vulnerabilities or VulnerabilityOutputMode
- The vulnerability_output parameter on save_console_report/print_sbom/print_summary_section

Reference for CLI gating pattern — already exists in cli.rs lines 83-95 as cfg_attr(feature = "cn-release", ...). Follow same plain cfg form (NOT cfg_attr — cfg_attr is for conditional attributes; we want conditional ITEMS).

Strip script reference (scripts/strip_vulnerability.sh steps 7-8) shows what parameter removals are needed for formatter signatures. When gating the mode parameter, use the cfg-on-parameter syntax which Rust supports natively.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Gate vuln-related enums and Args fields in src/cli.rs</name>
  <files>src/cli.rs, src/main.rs</files>
  <read_first>src/cli.rs, src/main.rs</read_first>
  <action>
Apply cfg(feature = "internal") to the three enums and their impls, plus the eight Args fields.

Enums and impls to gate — apply the cfg attribute immediately above each item:

1. pub enum MinSeverity (currently ~lines 29-50, the Low/Medium/High/Critical enum with ValueEnum derive)
2. impl MinSeverity (the impl block following MinSeverity, contains to_level method)
3. pub enum VulnerabilityOutputMode (Summary, Tree, Detailed) (~lines 52-60)
4. pub enum SbomMode (Complete, VulnerableOnly) (~lines 72-78)

Args struct fields to gate — apply cfg(feature = "internal") immediately above each #[arg(...)] line for these eight fields. Do NOT remove the #[arg(...)] attribute:

1. check_vulnerabilities: bool
2. min_severity: MinSeverity
3. vulnerability_timeout: u64
4. vulnerability_output: VulnerabilityOutputMode
5. cache_ttl: u64
6. clear_cache: bool
7. max_vulns_per_severity (verify exact type by reading cli.rs)
8. sbom_mode: SbomMode

Pattern for each Args field:

    #[cfg(feature = "internal")]
    #[arg(long, action = ArgAction::Set, default_value_t = false)]
    pub check_vulnerabilities: bool,

Do NOT touch the cfg_attr(feature = "cn-release", ...) annotation on the Args struct itself (lines 83-95) — that is a separate, existing feature gate that stays unchanged.

Do NOT gate TreeStyle enum or other non-vuln Args fields. Only the eight listed fields and three listed enums.

If any Default impl exists for Args that names the gated fields explicitly, those initializers must also be gated with cfg(feature = "internal"). Search for impl Default for Args and gate any vuln-field initializers found.

Cross-file callsite fixup in src/main.rs: After gating the Args fields, run:

    grep -n "args\.\(check_vulnerabilities\|min_severity\|vulnerability_timeout\|vulnerability_output\|cache_ttl\|clear_cache\|max_vulns_per_severity\|sbom_mode\)" src/main.rs

Plan 01 already wrapped the main scanning block in cfg(feature = "internal"). For any matches OUTSIDE that wrapped block (e.g., calls passing args.vulnerability_output to a formatter), wrap the offending statement in its own cfg(feature = "internal") block. If a single function call passes BOTH unconditional and gated args, you may need two parallel call expressions behind opposite cfg gates: one that calls with the gated args (when feature is on) and one without (when feature is off). Plan 03 task 2 will further refine these formatter signatures, so it is acceptable to leave a few unresolved name errors at end of this task — they will be cleaned up in task 2.
  </action>
  <verify>
    <automated>grep -B1 'pub enum MinSeverity' src/cli.rs | grep -q 'cfg(feature = "internal")' && grep -B1 'pub enum VulnerabilityOutputMode' src/cli.rs | grep -q 'cfg(feature = "internal")' && grep -B1 'pub enum SbomMode' src/cli.rs | grep -q 'cfg(feature = "internal")' && COUNT=$(grep -c 'cfg(feature = "internal")' src/cli.rs); if [ "$COUNT" -ge 11 ]; then echo OK; else echo "FAIL count=$COUNT (expected at least 11)"; exit 1; fi</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'cfg(feature = "internal")' src/cli.rs returns at least 11 (3 enums + 1 impl + 8 fields, allowing minor variance)
    - Lines containing pub enum MinSeverity, pub enum VulnerabilityOutputMode, pub enum SbomMode are each preceded by the cfg attribute
    - Each of the 8 vuln-related Args fields' #[arg(...)] line is preceded by cfg(feature = "internal")
    - cargo build --release --features internal exits 0
  </acceptance_criteria>
  <done>cli.rs has cfg(feature = "internal") on the three vuln enums, the MinSeverity impl, and all eight vuln-related Args fields. main.rs callsites passing the gated Args fields to formatters are inside cfg-gated blocks. Internal build still compiles.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Gate vulnerability branches in formatters and finalize main.rs callsites</name>
  <files>src/formats/cyclonedx.rs, src/formats/spdx.rs, src/formats/console.rs, src/main.rs</files>
  <read_first>src/formats/cyclonedx.rs, src/formats/spdx.rs, src/formats/console.rs, src/main.rs, scripts/strip_vulnerability.sh</read_first>
  <action>
Apply cfg(feature = "internal") to formatter imports, supporting structs, fields, functions, and call-site branches that touch vulnerability data. Use scripts/strip_vulnerability.sh (steps 7-8) as a reference for which function-signature parameters need gating.

src/formats/cyclonedx.rs — apply gates at:

1. Top-of-file imports: gate use crate::cli::SbomMode and VulnerabilitySeverity (likely in use crate::models import block — split the import so non-vuln names stay unconditional and VulnerabilitySeverity is gated separately).

2. CycloneDXDocument struct — gate the pub vulnerabilities field (~line 41) with cfg(feature = "internal"). Keep any serde attributes already on the field.

3. Gate ALL of these supporting structs (~lines 180-243) with cfg(feature = "internal") above each derive:
   - CycloneDXVulnerability
   - CycloneDXVulnerabilitySource
   - CycloneDXVulnerabilityRating
   - CycloneDXVulnerabilityReference
   - CycloneDXVulnerabilityAffect

4. Gate the build_cyclonedx_vulnerabilities function (~lines 248-393) with cfg(feature = "internal").

5. The convert_to_cyclonedx function takes mode: &SbomMode and inside it does SbomMode-based filtering. Use the cfg-on-parameter pattern (Rust supports cfg attributes on function parameters):

       pub fn convert_to_cyclonedx(
           sbom: &Sbom,
           // other unconditional params
           #[cfg(feature = "internal")] mode: &SbomMode,
       ) -> CycloneDXDocument {
           // unconditional body
           #[cfg(feature = "internal")]
           let filtered: Vec<_> = match mode { /* existing filter logic */ };
           // ...
       }

6. In convert_to_cyclonedx, where the constructed CycloneDXDocument literal sets vulnerabilities: ..., that field assignment must be wrapped in cfg(feature = "internal"). Easiest approach: switch the document construction to use ..Default::default() and add the vulnerabilities field via a gated post-construction assignment (only if the type has Default). If Default is not derivable, use two parallel struct literals under opposite cfg gates.

src/formats/spdx.rs — apply gates at:

1. use crate::cli::SbomMode (line 1) — gate with cfg(feature = "internal").
2. VulnerabilitySeverity import if present — gate it.
3. The Add vulnerability references loop in create_external_refs (~lines 223-241) — wrap with cfg(feature = "internal") block braces.
4. The SbomMode filtering block inside convert_to_spdx (~lines 558-562) — wrap with cfg(feature = "internal") braces.
5. Functions print_spdx_json, print_spdx_tag_value, convert_to_spdx, save_spdx_json, save_spdx_tag_value that take mode: &SbomMode — gate that parameter with cfg(feature = "internal") on the parameter (same pattern as cyclonedx).

src/formats/console.rs — apply gates at:

1. Imports — split use crate::cli::{TreeStyle, VulnerabilityOutputMode} into:

       use crate::cli::TreeStyle;
       #[cfg(feature = "internal")]
       use crate::cli::VulnerabilityOutputMode;

   Split use crate::models::{Dependency, DependencyGraph, DependencyNode, DependencyRelationship, Sbom, Vulnerability, VulnerabilitySeverity} into:

       use crate::models::{Dependency, DependencyGraph, DependencyNode, DependencyRelationship, Sbom};
       #[cfg(feature = "internal")]
       use crate::models::{Vulnerability, VulnerabilitySeverity};

2. Gate function count_unique_cves with cfg(feature = "internal").
3. Gate function print_vulnerabilities_hierarchical with cfg(feature = "internal").
4. Gate any helper function for severity formatting (search by grep "fn .*[Ss]everity" — gate any function whose body uses VulnerabilitySeverity).
5. In print_sbom/save_console_report/print_summary_section, the vulnerability_output: &VulnerabilityOutputMode parameter — gate that parameter (cfg-on-parameter pattern). Inside each function body, wrap any block referencing dep.vulnerabilities, VulnerabilityOutputMode, or vuln rendering in cfg(feature = "internal") braces.

Cross-file callsite fixup in src/main.rs: After gating function signatures, the call sites in src/main.rs must match. Run:

    grep -n "save_console_report\|print_spdx_json\|print_cyclonedx_json\|save_cyclonedx_json\|save_spdx_json\|save_spdx_tag_value" src/main.rs

For each call that passes a gated arg (e.g., args.vulnerability_output, args.sbom_mode, &mode_value), the call site must use the matching cfg-on-arg syntax. Pattern:

    save_console_report(
        &sbom,
        &dep_graph,
        // unconditional args
        #[cfg(feature = "internal")] &args.vulnerability_output,
    )?;

OR, if simpler, wrap the entire call expression in a cfg-gated block with a parallel non-internal call below it under opposite cfg. Pick the cfg-on-arg pattern when the call diff is small (recommended); use parallel calls when the diff is large.

Final verification: After this task, BOTH builds must compile:
- cargo build --release exits 0
- cargo build --release --features internal exits 0

And the public binary must have no vuln strings:
- ./target/release/radeis_sc2sbom --help piped through grep for vulner|cwe|cvss|cache-ttl returns nothing.

Run:

    cargo build --release && ./target/release/radeis_sc2sbom --help | grep -iE 'vulner|cwe|cvss|cache-ttl' && echo "FAIL: leak detected" && exit 1 || echo "OK: clean"
  </action>
  <verify>
    <automated>cargo build --release 2>&1 | tail -3 | grep -q 'Finished' && cargo build --release --features internal 2>&1 | tail -3 | grep -q 'Finished' && (./target/release/radeis_sc2sbom --help 2>/dev/null | grep -iE 'vulner|cwe|cvss|cache-ttl' && echo "FAIL leak" && exit 1 || echo OK)</automated>
  </verify>
  <acceptance_criteria>
    - cargo build --release exits 0 (no flags, public build)
    - cargo build --release --features internal exits 0 (internal build)
    - ./target/release/radeis_sc2sbom --help piped through grep -iE 'vulner|cwe|cvss|cache-ttl' returns no matches (i.e., grep exits non-zero)
    - grep -c 'cfg(feature = "internal")' src/formats/cyclonedx.rs returns at least 8 (imports + 5 structs + function + field + convert mode)
    - grep -c 'cfg(feature = "internal")' src/formats/spdx.rs returns at least 5
    - grep -c 'cfg(feature = "internal")' src/formats/console.rs returns at least 5
    - cargo test --features internal compiles (does NOT need to pass yet — that is plan 04 territory once test gate is set)
  </acceptance_criteria>
  <done>Public build compiles with zero vuln-related strings in --help output. Internal build still compiles. All formatter vulnerability emission and SbomMode filtering branches are behind cfg(feature = "internal"). main.rs call sites match formatter signatures via cfg-on-arg syntax.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public binary CLI surface | --help output is the most visible inspection vector for an open-source distribution; vuln-related strings here directly violate the "no vulnerability code in public binary" goal |
| Formatter output surface | CycloneDX/SPDX/console output formats reach end-users; vuln serialization branches must not be reachable from public-build code paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-06 | Information Disclosure | Public binary --help leaks vulnerability scanning capability via flag names | mitigate | cfg(feature = "internal") on each of 8 Args fields ensures clap does not register them; verified by grep on --help output |
| T-10-07 | Information Disclosure | Public binary contains CycloneDXVulnerability supporting structs as dead types in binary | mitigate | cfg(feature = "internal") on each supporting struct ensures rustc never emits them in non-internal compilation |
| T-10-08 | Tampering | Formatter signature changes (mode parameter gating) risk breaking call sites | mitigate | cfg-on-parameter syntax keeps signatures aligned across feature states; main.rs callsites updated with matching cfg-on-arg syntax in same task |
</threat_model>

<verification>
- cargo build --release exits 0
- cargo build --release --features internal exits 0
- ./target/release/radeis_sc2sbom --help | grep -iE 'vulner|cwe|cvss|cache-ttl' returns nothing
- ./target/release/radeis_sc2sbom --help shows the cn-release annotation if cn-release feature is set (existing behavior preserved)
</verification>

<success_criteria>
- 8 Args fields and 3 enums in cli.rs are cfg-gated
- All formatter vulnerability emission branches are cfg-gated
- Public binary compiles and has zero vuln strings in --help
- Internal binary compiles with full vuln stack intact
</success_criteria>

<output>
After completion, create .planning/phases/10-internal-feature-gate/10-03-SUMMARY.md documenting which formatter functions had parameter gates and any cross-file call-site adjustments made in main.rs.
</output>
