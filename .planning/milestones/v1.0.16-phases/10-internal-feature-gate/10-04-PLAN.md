---
phase: 10-internal-feature-gate
plan: 04
type: execute
wave: 4
depends_on: ["10-03"]
files_modified:
  - src/vulnerability/cwe_scanner.rs
  - src/vulnerability/mod.rs
  - tests/all_tests.rs
  - tests/scanner_tests/deduplication_tests.rs
  - tests/format_tests/spdx_tests.rs
  - tests/format_tests/cyclonedx_tests.rs
  - tests/parser_tests/c_tests.rs
  - tests/parser_tests/conan_tests.rs
  - tests/parser_tests/ros_tests.rs
  - tests/model_tests/dependency_tests.rs
  - tests/model_tests/sbom_tests.rs
  - tests/classifier_tests/scope_filter_tests.rs
  - tests/classifier_tests/autosar_classification_tests.rs
  - tests/integration_tests/mcu_project_tests.rs
  - tests/integration_tests/autosar_e2e_tests.rs
  - tests/integration_tests/scope_filtering_integration_tests.rs
  - .github/workflows/build-release.yml
  - scripts/strip_vulnerability.sh
autonomous: true
requirements: [GATE-03, GATE-04]
must_haves:
  truths:
    - "src/vulnerability/cwe_scanner.rs exists as a stub for Phase 11"
    - "Public-build cargo test passes (no vuln tests run, no compile errors)"
    - "Internal-build cargo test --features internal passes (vuln tests run as before)"
    - "build-release.yml internal build steps include --features internal"
    - "strip_vulnerability.sh has a comment confirming cwe_scanner.rs is covered by the directory removal"
  artifacts:
    - path: "src/vulnerability/cwe_scanner.rs"
      provides: "Empty stub for Phase 11 lexical scanner"
      min_lines: 2
    - path: "tests/all_tests.rs"
      provides: "Gated vulnerability_tests module"
      contains: "cfg(feature = \"internal\")"
    - path: ".github/workflows/build-release.yml"
      provides: "Internal CI build with --features internal"
      contains: "--features internal"
  key_links:
    - from: "src/vulnerability/mod.rs"
      to: "src/vulnerability/cwe_scanner.rs"
      via: "pub mod cwe_scanner"
      pattern: "pub mod cwe_scanner"
    - from: "tests/all_tests.rs"
      to: "tests/vulnerability_tests/mod.rs"
      via: "cfg(feature=\"internal\") gated mod declaration"
      pattern: "cfg\\(feature = \"internal\"\\).*\\n.*mod vulnerability_tests"
---

<objective>
Create the cwe_scanner.rs Phase 11 stub, gate the test harness so vulnerability_tests only run with --features internal, migrate test-side construction sites to ..Default::default(), update CI workflows to build with --features internal, and add a strip script comment confirming cwe_scanner.rs coverage.

Purpose: Closes GATE-03 (lexical scanner code is gated) by creating the stub inside the already-gated module subtree. Closes GATE-04 (cargo test passes without internal feature) by gating the vulnerability_tests module and migrating ~58 test construction sites. Aligns CI with the new feature flag.

Output: cargo test passes; cargo test --features internal passes; CI builds use the internal feature; strip script remains in sync.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-internal-feature-gate/10-CONTEXT.md
@.planning/phases/10-internal-feature-gate/10-RESEARCH.md
@.planning/phases/10-internal-feature-gate/10-PATTERNS.md
@.planning/phases/10-internal-feature-gate/10-03-SUMMARY.md

<interfaces>
src/vulnerability/mod.rs current state (lines 1-6):

    pub mod fix_recommendations;
    pub mod nvd;
    pub mod osv;

    pub use osv::{clear_vulnerability_cache, query_vulnerabilities_batch, OsvProvider};
    pub use nvd::enrich_cwe_ids;

tests/all_tests.rs lines 25-26 (per RESEARCH):

    #[path = "vulnerability_tests/mod.rs"]
    mod vulnerability_tests;

Files with explicit `vulnerabilities:` field in tests/ (58 occurrences across 13 files per RESEARCH):
- tests/scanner_tests/deduplication_tests.rs (6 occurrences)
- tests/format_tests/spdx_tests.rs (16 occurrences)
- tests/format_tests/cyclonedx_tests.rs (6 occurrences)
- tests/parser_tests/c_tests.rs (2 occurrences)
- tests/parser_tests/conan_tests.rs (1 occurrence)
- tests/parser_tests/ros_tests.rs (6 occurrences)
- tests/model_tests/dependency_tests.rs (4 occurrences)
- tests/model_tests/sbom_tests.rs (2 occurrences)
- tests/classifier_tests/scope_filter_tests.rs (1 occurrence)
- tests/classifier_tests/autosar_classification_tests.rs (1 occurrence)
- tests/integration_tests/mcu_project_tests.rs (2 occurrences)
- tests/integration_tests/autosar_e2e_tests.rs (1 occurrence)
- tests/integration_tests/scope_filtering_integration_tests.rs (10 occurrences)

Migration pattern (canonical reference is src/models/dependency.rs Dependency::new() ~lines 384-390): replace explicit `vulnerabilities: vec![]` (or similar) with the trailing `..Default::default()` form.

.github/workflows/build-release.yml — cargo build --release commands at lines 251 and 382 (per RESEARCH); also a validate-sbom step around line 444 that should run the internal-built binary.

scripts/strip_vulnerability.sh step 1 (lines 24-25): rm -rf src/vulnerability/. The new cwe_scanner.rs is automatically covered by this directory removal; the change is a comment only (no new rm command).

For format_tests that import SbomMode (e.g., tests/format_tests/cyclonedx_tests.rs imports SbomMode at line 1): SbomMode is now cfg-gated in cli.rs (plan 03). Test files in tests/format_tests/ that use SbomMode must either:
(a) be gated with #![cfg(feature = "internal")] at the top, OR
(b) drop the SbomMode import and switch to formatter calls that omit the SbomMode parameter (matches the public-build formatter signature post plan 03).
The decision: pick option (a) — gate the affected test files at the file level with #![cfg(feature = "internal")] inner attribute. This is consistent with D-09's gate-the-whole-vulnerability-test-module approach and keeps the test bodies unchanged.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create cwe_scanner.rs stub, register it in vulnerability/mod.rs, and gate vulnerability_tests in tests/all_tests.rs</name>
  <files>src/vulnerability/cwe_scanner.rs, src/vulnerability/mod.rs, tests/all_tests.rs</files>
  <read_first>src/vulnerability/mod.rs, tests/all_tests.rs</read_first>
  <action>
Create the new stub file and wire it in.

**File 1 — Create src/vulnerability/cwe_scanner.rs (new file)** with this exact content:

    // Phase 11: lexical CWE scanner — implementation pending
    // This file is the landing zone for the C/C++ static analysis scanner (SCAN-01..SCAN-05)

That is the entire file content (two comment lines). No code, no functions, no structs.

**File 2 — Update src/vulnerability/mod.rs** to add the module declaration. Insert at the top of the file, before the existing `pub mod fix_recommendations;` line:

    pub mod cwe_scanner;

Resulting top of file:

    pub mod cwe_scanner;
    pub mod fix_recommendations;
    pub mod nvd;
    pub mod osv;

    pub use osv::{clear_vulnerability_cache, query_vulnerabilities_batch, OsvProvider};
    pub use nvd::enrich_cwe_ids;

No cfg annotation needed here — the entire vulnerability module is already gated at lib.rs and main.rs by plan 01.

**File 3 — Update tests/all_tests.rs** to gate the vulnerability_tests module declaration. Find:

    #[path = "vulnerability_tests/mod.rs"]
    mod vulnerability_tests;

Insert `#[cfg(feature = "internal")]` immediately above. Result:

    #[cfg(feature = "internal")]
    #[path = "vulnerability_tests/mod.rs"]
    mod vulnerability_tests;

Do NOT touch tests/vulnerability_tests/mod.rs itself — gating in all_tests.rs is sufficient (D-09 requirement is satisfied).

After these three edits:
- cargo build --release --features internal must compile (cwe_scanner.rs is now compiled into the gated module — empty file is valid Rust).
- cargo test --features internal must compile vulnerability_tests as before.
- cargo test (no flags) must SKIP vulnerability_tests at compile time (cfg gate). Other test failures may still exist due to construction-site issues — those are task 2's scope.
  </action>
  <verify>
    <automated>test -f src/vulnerability/cwe_scanner.rs && grep -q "Phase 11" src/vulnerability/cwe_scanner.rs && grep -q "^pub mod cwe_scanner;$" src/vulnerability/mod.rs && grep -B1 'mod vulnerability_tests;' tests/all_tests.rs | grep -q 'cfg(feature = "internal")' && cargo build --release --features internal 2>&1 | tail -3 | grep -q 'Finished' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - File src/vulnerability/cwe_scanner.rs exists
    - grep -c "Phase 11" src/vulnerability/cwe_scanner.rs returns at least 1
    - grep -c "^pub mod cwe_scanner;$" src/vulnerability/mod.rs returns 1
    - The line `mod vulnerability_tests;` in tests/all_tests.rs is preceded by `#[cfg(feature = "internal")]` (combined with the existing `#[path = ...]` attribute)
    - cargo build --release --features internal exits 0
    - cargo build --release exits 0 (public build still compiles)
  </acceptance_criteria>
  <done>cwe_scanner.rs stub exists; vulnerability/mod.rs declares it; tests/all_tests.rs gates vulnerability_tests with cfg(feature = "internal"); both builds compile.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Migrate test-side construction sites and gate format-test files using SbomMode</name>
  <files>tests/scanner_tests/deduplication_tests.rs, tests/format_tests/spdx_tests.rs, tests/format_tests/cyclonedx_tests.rs, tests/parser_tests/c_tests.rs, tests/parser_tests/conan_tests.rs, tests/parser_tests/ros_tests.rs, tests/model_tests/dependency_tests.rs, tests/model_tests/sbom_tests.rs, tests/classifier_tests/scope_filter_tests.rs, tests/classifier_tests/autosar_classification_tests.rs, tests/integration_tests/mcu_project_tests.rs, tests/integration_tests/autosar_e2e_tests.rs, tests/integration_tests/scope_filtering_integration_tests.rs</files>
  <read_first>src/models/dependency.rs (lines 380-395, the Dependency::new helper as canonical migration pattern)</read_first>
  <action>
Run discovery first (do not trust the file list — re-grep):

    grep -rn "vulnerabilities:" tests/ --include="*.rs" | grep -v "tests/vulnerability_tests/"

For each match outside tests/vulnerability_tests/, migrate the construction site. Same pattern as plan 02 task 2:

1. Locate the full Dependency { ... } struct literal containing the vulnerabilities: line.
2. Delete the `vulnerabilities: <expr>,` line entirely.
3. Verify or add `..Default::default()` at the end of the struct literal.

Reference (BEFORE):

    fn make_dep(name: &str, version: &str, ecosystem: &str) -> Dependency {
        Dependency {
            name: name.to_string(),
            version: version.to_string(),
            ecosystem: ecosystem.to_string(),
            source: DependencySource::LockFile,
            vulnerabilities: vec![],
            // ... explicit fields ...
        }
    }

AFTER:

    fn make_dep(name: &str, version: &str, ecosystem: &str) -> Dependency {
        Dependency {
            name: name.to_string(),
            version: version.to_string(),
            ecosystem: ecosystem.to_string(),
            source: DependencySource::LockFile,
            ..Default::default()
        }
    }

Special case for tests/format_tests/cyclonedx_tests.rs and tests/format_tests/spdx_tests.rs — these files import SbomMode (which is now gated in cli.rs from plan 03). Two-step handling:

A. Migrate construction sites as above (remove `vulnerabilities:` lines, add `..Default::default()`).

B. If after construction migration the test file STILL contains references to SbomMode, MinSeverity, VulnerabilityOutputMode, or `dep.vulnerabilities` (field READ) — add an inner cfg attribute at the very top of the file (before any `use` statements):

    #![cfg(feature = "internal")]

This makes the entire test file compile only when --features internal is active. Apply this top-level inner cfg ONLY to format_tests files that genuinely cannot compile without internal feature symbols. After construction-site migration, many format_tests may not need this gate — verify by attempting `cargo test --no-run` after migration. If it compiles without the inner cfg, do not add it.

For the other 11 test files (parser_tests/*, model_tests/*, classifier_tests/*, integration_tests/*, scanner_tests/deduplication_tests.rs): construction-site migration alone should be sufficient — these files do not import SbomMode/VulnerabilityOutputMode/Vulnerability per RESEARCH. Verify by grep after migration:

    grep -lE "SbomMode|VulnerabilityOutputMode|MinSeverity|::Vulnerability\b" tests/ -r --include="*.rs" | grep -v "tests/vulnerability_tests/" | grep -v "tests/format_tests/"

If any non-format_tests files match, add `#![cfg(feature = "internal")]` to each of them. If only format_tests files match, gate just those.

Final verification:

    cargo test --no-run                 # public build test compile must succeed
    cargo test --features internal --no-run  # internal build test compile must succeed
    cargo test                          # public test run (no vuln tests execute)
    cargo test --features internal      # full test suite

Both `cargo test` and `cargo test --features internal` must exit 0.
  </action>
  <verify>
    <automated>RESULT=$(grep -rn "vulnerabilities: vec!\|vulnerabilities: Vec::new" tests/ --include="*.rs" | grep -v "tests/vulnerability_tests/" || true); if [ -n "$RESULT" ]; then echo "FAIL: construction sites remain"; echo "$RESULT"; exit 1; fi && cargo test --no-run 2>&1 | tail -3 | grep -q 'Finished' && cargo test --features internal --no-run 2>&1 | tail -3 | grep -q 'Finished' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "vulnerabilities: vec!" tests/ --include="*.rs" | grep -v "tests/vulnerability_tests/" returns no lines
    - grep -rn "vulnerabilities: Vec::new" tests/ --include="*.rs" | grep -v "tests/vulnerability_tests/" returns no lines
    - cargo test --no-run exits 0 (public test compile)
    - cargo test --features internal --no-run exits 0 (internal test compile)
    - cargo test exits 0 (public test run)
    - cargo test --features internal exits 0 (internal test run — full suite as before)
    - For any test file that still imports SbomMode, VulnerabilityOutputMode, MinSeverity, or Vulnerability: file begins with `#![cfg(feature = "internal")]` (verifiable: `head -3 <file> | grep -q 'cfg(feature = "internal")'`)
  </acceptance_criteria>
  <done>All test-side construction sites migrated to `..Default::default()`. Format-test files that depend on gated symbols are themselves gated with #![cfg(feature = "internal")]. Both feature states pass cargo test cleanly.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update CI workflow and strip script comment</name>
  <files>.github/workflows/build-release.yml, scripts/strip_vulnerability.sh</files>
  <read_first>.github/workflows/build-release.yml, scripts/strip_vulnerability.sh</read_first>
  <action>
**File 1 — .github/workflows/build-release.yml** — add `--features internal` to the internal-release cargo build commands.

Search the file for `cargo build --release` occurrences. Per RESEARCH, expected at lines 251, 382, and 444 (validate-sbom step):

    grep -n "cargo build --release" .github/workflows/build-release.yml

For EACH match in this file (build-release.yml is the INTERNAL release workflow per D-15), append `--features internal` immediately after `--release` and before any `--target` flag:

BEFORE:
    cargo build --release --target ${{ matrix.target }}

AFTER:
    cargo build --release --features internal --target ${{ matrix.target }}

Apply this transform to every `cargo build --release` line in the file. If a line is already `cargo build --release --features internal ...`, leave it.

Also check the validate-sbom step's SBOM generation command (line ~444). If the validate-sbom step runs the binary with vuln-related flags (e.g., `--check-vulnerabilities`), the binary it consumes must have been built with `--features internal`. Confirm the artifact source is the build job that has been updated; no separate command change is needed in validate-sbom if it consumes the same artifact.

Do NOT modify .github/workflows/public-release.yml — that is the PUBLIC release workflow and must NOT have --features internal (it relies on the strip script + default cargo build).

**File 2 — scripts/strip_vulnerability.sh** — add a comment confirming cwe_scanner.rs coverage. Find the existing block (around lines 24-25):

    echo "  [1] Removing src/vulnerability/ directory..."
    rm -rf src/vulnerability/

Insert a comment immediately above this block (D-14 documentation):

    # Note: src/vulnerability/cwe_scanner.rs (Phase 10 stub for Phase 11 lexical scanner)
    # is covered by the directory removal above — no separate rm command needed.

Do NOT add an additional rm command. The directory removal already handles cwe_scanner.rs. The comment satisfies D-14.

Final verification:
- grep -c "cargo build --release --features internal" .github/workflows/build-release.yml returns at least 2 (lines 251 and 382 minimum).
- grep -c "cwe_scanner.rs" scripts/strip_vulnerability.sh returns at least 1.
- Make a final whole-phase check:

      cargo build --release && \
      cargo build --release --features internal && \
      cargo test && \
      cargo test --features internal && \
      ./target/release/radeis_sc2sbom --help | grep -iE 'vulner|cwe|cvss|cache-ttl' && echo "FAIL leak" || echo "OK"
  </action>
  <verify>
    <automated>COUNT=$(grep -c "cargo build --release --features internal" .github/workflows/build-release.yml); if [ "$COUNT" -lt 2 ]; then echo "FAIL: --features internal added to fewer than 2 build commands ($COUNT)"; exit 1; fi && grep -q "cwe_scanner.rs" scripts/strip_vulnerability.sh && cargo build --release 2>&1 | tail -3 | grep -q Finished && cargo build --release --features internal 2>&1 | tail -3 | grep -q Finished && cargo test 2>&1 | tail -3 | grep -qE 'test result.*ok|0 failed' && (./target/release/radeis_sc2sbom --help 2>/dev/null | grep -iE 'vulner|cwe|cvss|cache-ttl' && echo "FAIL leak" && exit 1 || echo OK)</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "cargo build --release --features internal" .github/workflows/build-release.yml returns at least 2
    - .github/workflows/public-release.yml does NOT contain "--features internal" (verifiable: `grep -c "features internal" .github/workflows/public-release.yml` returns 0)
    - grep -c "cwe_scanner.rs" scripts/strip_vulnerability.sh returns at least 1
    - cargo build --release exits 0
    - cargo build --release --features internal exits 0
    - cargo test exits 0
    - cargo test --features internal exits 0
    - ./target/release/radeis_sc2sbom --help piped through grep -iE 'vulner|cwe|cvss|cache-ttl' returns no matches (grep exits non-zero)
  </acceptance_criteria>
  <done>build-release.yml builds with --features internal. public-release.yml unchanged. strip script has cwe_scanner.rs comment. Full phase verification matrix passes: both feature states build, both feature states test, public binary --help is clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test compilation surface | Test code must compile in BOTH feature states; failures here mean a developer cannot run tests on a public-build clone |
| CI release artifacts | build-release.yml produces internal binaries; public-release.yml produces stripped public binaries; misalignment between them and the feature flags creates broken releases |
| Phase 11 landing zone | The cwe_scanner.rs stub is the boundary where Phase 11's scanner code will land; placing it inside the already-gated module means Phase 11 inherits the gate for free |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Tampering | Test files leak gated symbol references and prevent public-build compilation | mitigate | Top-level #![cfg(feature = "internal")] on format_tests files that import SbomMode/Vulnerability symbols; construction-site migration for non-format tests |
| T-10-10 | Information Disclosure | Phase 11 scanner code lands in a non-gated location and leaks into public binary | mitigate | cwe_scanner.rs lives inside src/vulnerability/ which is already gated at lib.rs/main.rs (plan 01); strip script removes the whole directory in public branch (D-14) |
| T-10-11 | Tampering | CI workflow drift between public-release.yml and build-release.yml causes feature-flag mismatch | mitigate | This task explicitly modifies only build-release.yml; public-release.yml acceptance check verifies it is NOT modified |
</threat_model>

<verification>
Full phase verification matrix:

    cargo build --release                          # public build compiles
    cargo build --release --features internal      # internal build compiles
    cargo test                                      # public tests pass (no vuln tests run)
    cargo test --features internal                 # internal tests pass (full suite)
    ./target/release/radeis_sc2sbom --help | grep -iE 'vulner|cwe|cvss|cache-ttl'  # MUST be empty

CI checks:
- build-release.yml has at least 2 occurrences of `--features internal`
- public-release.yml has 0 occurrences of `--features internal`
- strip_vulnerability.sh has a comment mentioning cwe_scanner.rs
</verification>

<success_criteria>
- src/vulnerability/cwe_scanner.rs stub exists
- src/vulnerability/mod.rs declares pub mod cwe_scanner
- tests/all_tests.rs gates vulnerability_tests module with cfg(feature = "internal")
- All test-side construction sites migrated
- Format-test files importing gated symbols have #![cfg(feature = "internal")]
- build-release.yml uses --features internal in all internal cargo build commands
- public-release.yml is unchanged
- strip_vulnerability.sh has D-14 comment
- Full matrix (both feature states for build and test) passes
- Public binary --help has no vuln strings
</success_criteria>

<output>
After completion, create .planning/phases/10-internal-feature-gate/10-04-SUMMARY.md documenting:
- Number of test construction sites migrated per file
- List of test files that received #![cfg(feature = "internal")] inner attribute
- Number of cargo build --release lines updated in build-release.yml
- Final phase verification matrix output
</output>
