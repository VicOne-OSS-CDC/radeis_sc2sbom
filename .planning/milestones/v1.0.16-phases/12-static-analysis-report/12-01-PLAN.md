---
phase: 12-static-analysis-report
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/format_tests/sast_report_tests.rs
  - tests/format_tests/mod.rs
autonomous: true
requirements: [RPT-01, RPT-02, RPT-03]
tags: [rust, testing, markdown, static-analysis, sbom]

must_haves:
  truths:
    - "Test file exists with failing tests for save_static_analysis_report behavior"
    - "Test file exists with failing test for SAST section in save_console_report"
    - "Test file exists with failing test for stderr disclaimer"
    - "Test module is registered in tests/format_tests/mod.rs gated by internal feature"
    - "SastFinding struct field names are documented (read from src/vulnerability/cwe_scanner.rs Phase 11 output)"
  artifacts:
    - path: "tests/format_tests/sast_report_tests.rs"
      provides: "Failing tests for RPT-01, RPT-02, RPT-03"
      min_lines: 80
      contains: "test_save_static_analysis_report"
    - path: "tests/format_tests/mod.rs"
      provides: "Re-export of new sast_report_tests module"
      contains: "sast_report_tests"
  key_links:
    - from: "tests/format_tests/sast_report_tests.rs"
      to: "src/vulnerability/cwe_scanner.rs"
      via: "use radeis_sc2sbom::vulnerability::cwe_scanner::SastFinding"
      pattern: "use radeis_sc2sbom::vulnerability::cwe_scanner::SastFinding"
    - from: "tests/format_tests/mod.rs"
      to: "tests/format_tests/sast_report_tests.rs"
      via: "pub mod sast_report_tests"
      pattern: "pub mod sast_report_tests"
---

<objective>
Create the failing test scaffolding for Phase 12's three requirements (RPT-01, RPT-02, RPT-03) BEFORE implementing the formatter. Tests must compile with `--features internal` and FAIL because `save_static_analysis_report` does not yet exist and the SAST section is not yet injected into `save_console_report`. This is the Wave 0 / RED step that downstream plans (12-02, 12-03) will turn GREEN.

Purpose: The Nyquist rule requires every `<verify>` in later tasks to point at a real test command. This plan creates those tests up front so executors can use TDD on plans 02 and 03.
Output: `tests/format_tests/sast_report_tests.rs` with three test functions that compile but fail; `tests/format_tests/mod.rs` registers the module under `#[cfg(feature = "internal")]`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-static-analysis-report/12-CONTEXT.md
@.planning/phases/12-static-analysis-report/12-RESEARCH.md
@.planning/phases/12-static-analysis-report/12-PATTERNS.md

@tests/format_tests/mod.rs
@tests/format_tests/cyclonedx_tests.rs

<interfaces>
<!-- SastFinding struct lives in src/vulnerability/cwe_scanner.rs (Phase 11 output). -->
<!-- Field names are at Claude's discretion in Phase 11 D-01. The expected fields per RESEARCH.md A1 are: -->
<!--   component_name: String -->
<!--   file_path: String or PathBuf -->
<!--   line: u32 or usize -->
<!--   cwe_id: u32 -->
<!--   function_name: String -->
<!-- Task 1 must `cat src/vulnerability/cwe_scanner.rs` (or read it) to confirm the actual field names before writing test helper. -->
<!-- If Phase 11 has not yet shipped cwe_scanner.rs, this plan documents the EXPECTED shape and tests reference that contract; -->
<!-- a TODO note in the test helper records which field names were assumed so 12-02 can correct on integration. -->

Expected SastFinding shape (from RESEARCH.md A1 + Phase 11 CONTEXT.md):
```rust
pub struct SastFinding {
    pub component_name: String,
    pub file_path: String,    // or PathBuf — verify in Task 1
    pub line: u32,            // or usize — verify in Task 1
    pub cwe_id: u32,
    pub function_name: String,
}
```

Function under test (does NOT yet exist — created in plan 12-02):
```rust
#[cfg(feature = "internal")]
pub fn save_static_analysis_report(
    project_name: &str,
    out_dir: &Path,
    findings: &[SastFinding],
) -> anyhow::Result<()>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Verify SastFinding struct shape and create test file</name>
  <files>tests/format_tests/sast_report_tests.rs</files>
  <read_first>
    - src/vulnerability/cwe_scanner.rs (REQUIRED — read to confirm exact field names. If file does not exist yet because Phase 11 has not shipped, note that explicitly in a comment at the top of the test file and use the expected shape from RESEARCH.md A1: component_name: String, file_path: String, line: u32, cwe_id: u32, function_name: String)
    - tests/format_tests/cyclonedx_tests.rs (test pattern analog — imports, helper functions, #[test] structure)
    - tests/format_tests/mod.rs (current module structure to mirror)
    - .planning/phases/12-static-analysis-report/12-CONTEXT.md (locked decisions D-01..D-11 — tests must encode these)
    - .planning/phases/12-static-analysis-report/12-PATTERNS.md (test file analog at lines 240–305)
  </read_first>
  <action>
Create `tests/format_tests/sast_report_tests.rs` with the following exact structure:

1. Top of file: `#![cfg(feature = "internal")]` (file-level gate so tests only compile under the internal feature).

2. Imports:
```rust
use radeis_sc2sbom::formats::save_static_analysis_report;
use radeis_sc2sbom::vulnerability::cwe_scanner::SastFinding;
use std::path::PathBuf;
use tempfile::TempDir;
```

3. Helper function `make_finding(component: &str, file: &str, line: u32, cwe_id: u32, func: &str) -> SastFinding` that constructs a `SastFinding` using the field names confirmed from `src/vulnerability/cwe_scanner.rs`. If `cwe_scanner.rs` does not exist yet, use the expected fields from the interfaces block above and add a `// TODO(12-02): verify field names against actual SastFinding struct after Phase 11 ships` comment.

4. Three `#[test]` functions (each MUST FAIL until plans 12-02 and 12-03 implement the code):

   a. `test_save_static_analysis_report_with_findings` — creates 3 findings across 2 components and 2 CWEs, calls `save_static_analysis_report("myproj", tmp_dir.path(), &findings)`, then reads the resulting file at `tmp_dir.path().join("myproj_static_analysis.md")` and asserts:
      - File contains `"# Static Analysis Report"` (H1)
      - File contains `"> **Note:** Pattern-based — complex data-flow vulnerabilities not covered."` (D-11 blockquote)
      - File contains `"| Component | CWE | Name | Count |"` (D-04 summary table header)
      - File contains the literal text `"## libfoo"` (D-05 component grouping for one of the seeded findings)
      - File contains the literal text `"### CWE-120"` (D-05 CWE subgrouping)
      - File contains a finding bullet matching the regex `^- src/.*:\d+ — \w+$` (D-05 bullet format `- file:line — function`)

   b. `test_save_static_analysis_report_zero_findings` — calls `save_static_analysis_report("myproj", tmp_dir.path(), &[])` and asserts:
      - File EXISTS at `tmp_dir.path().join("myproj_static_analysis.md")` (D-02 always written)
      - File contains `"No static analysis findings detected."` (D-03 + D-06)
      - File contains `"| Component | CWE | Name | Count |"` (D-03 summary headers still printed)
      - File contains `"| — | — | No static analysis findings detected. | — |"` (D-03 single note row)
      - File contains `"> **Note:** Pattern-based"` (D-11 disclaimer still embedded)

   c. `test_save_static_analysis_report_writes_correct_filename` — calls `save_static_analysis_report("alpha", tmp_dir.path(), &[])` and asserts that exactly the file `alpha_static_analysis.md` exists in tmp_dir (D-01 filename convention).

5. Add `tempfile = "3"` to `Cargo.toml` `[dev-dependencies]` if not already present. (Run `grep -E '^tempfile' Cargo.toml` first; only add if absent.)

DO NOT yet write tests for the SAST section in `_report.md` or the stderr disclaimer — those are Task 2 below. Keep this task scoped to the new dedicated file's behavior.

The tests SHOULD FAIL with a compile error like "cannot find function `save_static_analysis_report` in module `formats`" — this is correct RED state for the TDD cycle.
  </action>
  <verify>
    <automated>cargo build --features internal --tests 2>&amp;1 | grep -E "cannot find function .save_static_analysis_report." >/dev/null &amp;&amp; echo RED_OK || echo RED_MISSING</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/format_tests/sast_report_tests.rs` exists
    - `grep -c "^#\!\[cfg(feature = \"internal\")\]" tests/format_tests/sast_report_tests.rs` >= 1
    - `grep -c "fn test_save_static_analysis_report_with_findings" tests/format_tests/sast_report_tests.rs` == 1
    - `grep -c "fn test_save_static_analysis_report_zero_findings" tests/format_tests/sast_report_tests.rs` == 1
    - `grep -c "fn test_save_static_analysis_report_writes_correct_filename" tests/format_tests/sast_report_tests.rs` == 1
    - `grep -c "Pattern-based — complex data-flow vulnerabilities not covered" tests/format_tests/sast_report_tests.rs` >= 2 (asserted in both with-findings and zero-findings tests)
    - `grep -c "No static analysis findings detected." tests/format_tests/sast_report_tests.rs` >= 2
    - `grep -c "myproj_static_analysis.md" tests/format_tests/sast_report_tests.rs` >= 1
    - `grep -c "tempfile" Cargo.toml` >= 1 (dev-dependency present)
  </acceptance_criteria>
  <done>
    Test file compiles its dependencies (imports resolve to existing items in lib OR the file lints cleanly under `cargo build --features internal --tests` with the EXPECTED error of `save_static_analysis_report` not being found in `formats`). The error must be exactly that — not a syntax error, not a `SastFinding` import failure. Three #[test] functions present.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add tests for SAST section in _report.md and stderr disclaimer; register module</name>
  <files>tests/format_tests/sast_report_tests.rs, tests/format_tests/mod.rs</files>
  <read_first>
    - tests/format_tests/sast_report_tests.rs (just-created file from Task 1)
    - tests/format_tests/mod.rs (existing module declarations to extend)
    - src/formats/console.rs lines 1100-1140 (read save_console_report signature so the test calls it correctly)
    - .planning/phases/12-static-analysis-report/12-CONTEXT.md (D-07, D-08, D-09, D-10)
  </read_first>
  <action>
Step A — Extend `tests/format_tests/sast_report_tests.rs` with two more `#[test]` functions:

1. `test_console_report_includes_sast_section_with_findings` — builds an `Sbom` (use the `make_sbom`/`make_dep` helper pattern from `tests/format_tests/cyclonedx_tests.rs` lines 1–50; copy the helpers into this file as a private mod or inline functions). Constructs `sast_findings: Vec<SastFinding>` with 2 findings. Calls `save_console_report(&sbom, &out_path_str, &TreeStyle::Compact, &VulnerabilityOutputMode::Severity, 10, &[], false, true)` after temporarily setting up whatever variable holding sast_findings the implementation expects (per RESEARCH.md Open Question 2 approach (c), `findings` will be a variable in scope inside `save_console_report` — for testing, the test seeds the function via a thread-local OR the implementation in 12-03 wires the parameter through; **document this in a `// TODO(12-03)` comment** and write the test against the FUTURE signature: `save_console_report(..., args.check_vulnerabilities, &sast_findings)` with a trailing `&[SastFinding]` parameter — this is the contract 12-03 must implement). Reads the output file and asserts:
   - Output contains `"## Static Analysis Findings"` (D-07)
   - The `"## Static Analysis Findings"` header appears AFTER the CVE/Vulnerabilities section heading or content (D-07 ordering — assert that `output.find("## Static Analysis Findings") > output.find("Vulnerabilities")` if the CVE block is present, OR just verify the section is present when CVEs are absent)
   - Output contains the summary table header `"| Component | CWE | Name | Count |"` (D-08)
   - Output does NOT contain `"### CWE-"` H3 subheader inside the `## Static Analysis Findings` section (D-08 — summary only, no per-component file:line groups in `_report.md`). Verify by extracting the substring from `## Static Analysis Findings` to either end-of-file or next `## ` heading and asserting it does not contain `"### CWE-"`.

2. `test_console_report_includes_sast_section_zero_findings` — same setup but `sast_findings = Vec::new()`. Asserts:
   - Output contains `"## Static Analysis Findings"` (D-09 — section still present)
   - Output contains `"No static analysis findings"` (D-09 message)

NOTE: If the test cannot easily call `save_console_report` because of the planned signature change, add the test as `#[ignore = "wired in 12-03"]` so it compiles and is discoverable but does not run. Plan 12-03's first task will remove the `#[ignore]` attribute.

Step B — Add a stderr disclaimer test:

3. `test_save_static_analysis_report_emits_disclaimer_to_stderr` — Rust unit tests cannot easily capture stderr from `eprintln!`. Use the `gag` crate OR document this as a manual verification by emitting a comment `// MANUAL: cargo run --features internal -- <args> 2>&1 | grep "Pattern-based"`. Preferred: add the test marked `#[ignore = "stderr capture requires integration test harness"]` and instead create a separate integration check in plan 12-02's verify step that runs `cargo run --features internal --example sast_report_smoke 2>&1 | grep -F "Pattern-based — complex data-flow vulnerabilities not covered"`. If that's too heavy, drop the test and instead add a `grep` acceptance criterion in plan 12-02 against `console.rs` that the literal `eprintln!("Pattern-based — complex data-flow vulnerabilities not covered")` line is present.

   For this task, just write the test scaffolding with `#[ignore]` and leave the stderr verification to plan 12-02's `<acceptance_criteria>` grep on the source.

Step C — Update `tests/format_tests/mod.rs`. Read the current contents and append:
```rust
#[cfg(feature = "internal")]
pub mod sast_report_tests;
```
Place it after the existing `pub mod cyclonedx_tests;` line. Do NOT remove or reorder existing module declarations.
  </action>
  <verify>
    <automated>cargo build --features internal --tests 2>&amp;1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "fn test_console_report_includes_sast_section_with_findings" tests/format_tests/sast_report_tests.rs` == 1
    - `grep -c "fn test_console_report_includes_sast_section_zero_findings" tests/format_tests/sast_report_tests.rs` == 1
    - `grep -c "## Static Analysis Findings" tests/format_tests/sast_report_tests.rs` >= 2
    - `grep -v '^//' tests/format_tests/mod.rs | grep -c "pub mod sast_report_tests" ` == 1
    - `grep -B1 "pub mod sast_report_tests" tests/format_tests/mod.rs | grep -c "feature = \"internal\""` == 1
    - `cargo build --features internal --tests 2>&1` succeeds OR fails ONLY with errors of the form `cannot find function .save_static_analysis_report.` and/or `expected N arguments, found M` for `save_console_report` (the canonical RED-phase compilation errors). No syntax errors, no missing-import errors for `SastFinding`.
    - `cargo build --tests 2>&1` (NO --features internal) succeeds — the gate prevents the test module from compiling on default builds.
  </acceptance_criteria>
  <done>
    Five total #[test] functions in sast_report_tests.rs (3 from Task 1 + 2 from Task 2). Module registered in mod.rs under feature = "internal". Default build (no internal feature) compiles cleanly. Internal feature build fails with expected RED errors that plans 12-02 and 12-03 will resolve.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run:
```bash
cargo build --tests 2>&1 | tee /tmp/12-01-default-build.log
cargo build --features internal --tests 2>&1 | tee /tmp/12-01-internal-build.log
```

Expected:
- Default build: clean (no errors related to sast_report_tests — gated out)
- Internal build: fails with compilation errors of EXACTLY the forms:
  - `cannot find function \`save_static_analysis_report\` in module \`formats\``
  - (possibly) `this function takes N arguments but M were supplied` for save_console_report
- No errors of the form `unresolved import` for SastFinding (must succeed — Phase 11 must have shipped or contain a stub).

If `unresolved import` fires for SastFinding: STOP. Phase 11 has not shipped the struct; coordinate with the orchestrator to verify Phase 11 status before continuing.
</verification>

<success_criteria>
- `tests/format_tests/sast_report_tests.rs` exists with 5 `#[test]` functions encoding RPT-01, RPT-02, RPT-03 acceptance per locked decisions D-01..D-11
- `tests/format_tests/mod.rs` registers the new module under `#[cfg(feature = "internal")]`
- `tempfile` dev-dependency present in Cargo.toml
- Default `cargo build --tests` succeeds
- `cargo build --features internal --tests` fails with the expected RED errors and ONLY those errors
- All assertions reference EXACT strings from CONTEXT.md decisions (verbatim disclaimer text, table header, blockquote)
</success_criteria>

<output>
After completion, create `.planning/phases/12-static-analysis-report/12-01-SUMMARY.md` documenting:
- Confirmed SastFinding field names (read from `src/vulnerability/cwe_scanner.rs`) OR documented assumption if Phase 11 has not shipped
- The 5 test functions and which RPT requirement each covers
- The intentional RED-state compile errors that plans 12-02 and 12-03 will resolve
</output>
